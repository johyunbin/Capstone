It's curious to see the difference between professional programmers who have been programming all their lives using artificial intelligence versus those who are just starting. Those who are just starting have to use tools like Copilot or Courser, which are pretty good, but Cloud Code is another level. Cloud Code is my favorite. Cloud Code can get into the folder where all the files of a project of yours are, understand them in an intimate and deep way and start making complete changes with a precision that at this moment is the standard of most experienced programmers. What we are going to teach you today in this video is how to start using Cloud Code and how to get the most out of it, especially now that the rules and the prices of Cloud have changed and now Cloud Code is available for most subscribers. Anthropic announced that Cloud Code is now available with the Pro subscription. You no longer need to pay $100 a month or pay for each token to be able to use it. 

In this video I'm going to explain how to use Cloud Code with an existing code base and take the most advantage of it to have the best possible code only using artificial intelligence. To be able to use Cloud Code, the first thing we are going to do is go to docs .addropi .com and go to the Cloud Code section. We will see that the Cloud Code option is here and the first thing that appears is a general description of what Cloud Code is. And they say it's a coding tool based on agents created by Anthropic. In fact, this is the tool they use internally to be able to program. They say it's in beta, but the truth is that it's quite stable and quite secure to be able to code, so you can use it without worries. To install it, we see that there is a command here in the section that is in black and the first thing we see is that we need npm. npm is the package manager of node. If you don't know what node is or you don't know what npm is, I recommend the free programming course here in Platzi. 

So, we copy the command and go to the terminal. So, we go to the terminal and paste the command that we had in the paper cutout. Here we will see that the cloud installation process will start. In this case, I already had it installed and maybe if you don't have it installed, you will see some approval or verification steps. But in general, its installation is quite simple. Once it is installed, we can use it through the cloud command. Everything happens in Cloud Code within the terminal. So, the first thing we're going to see is that it's going to ask us to choose a text style. In this case, we have six options. I like the dark mode, so I'm going to select it. The second is the login method that we're going to use to be able to use Cloud Code. We'll see that there are two. One that is with the subscription to Cloud, which in this case they have two, which is Pro and Max. and the other one that is through the Anthropic console. What is the difference? 

With the subscription you can use not only Cloud Code, but also Cloud, the LLM and the desktop application that Cloud has. With Anthropic this is more business than anything else. What is done is to buy credits and those credits are used to generate the code in this case. I'm going to use option 1. which is the one I use regularly, or at least I personally use. I select it and here my browser will open. And in the browser I have to start a session within Cloud with my credentials. Here what I'm going to do is only authorize Cloud Code to use my account. So I give it to authorize and we go back to the terminal. And in this case we already see that the login was successful. And for us to continue, I have to press enter. Great. I already have the session started. Then what Cloud Code tells me are some pretty important security warnings. The first thing is that Cloud can make mistakes. So it is always necessary to review the answers that Cloud gives us. 

We don't have to trust 100 of what it generates. Although it can be very good, it always has to go through a human review. And second, from some security risks that are specified on that site, we should only use Cloud Code in code that we trust. These are just security warnings, we can continue. And we already have Cloud Code in our system. How do we know it's working? We can say, Hello. And in this case, it will start to respond. We will see that it is only an interface inside the terminal. This does not have a graphic interface like Cursor or Windsor or ChatGPT. No, this happens inside the terminal. So I see that this is something much more typical to the flow of development of a programmer, which is that we are always inside the terminal executing some command or even if we use a text editor that is inside the terminal, Cloud Code keeps us inside that environment. Great. 

So I'm going to exit Cloud for a moment to specify something. We see that I'm using the same code that was generated in a previous class in the cursor video that was to create an API with FASAPI. This is inside the Platzi repository under the name CursorAI. What I did was grab here, select and make a git clone. See? Here is the command. And that's the code I'm going to use for this demonstration. Within this code, we needed to do something too important, which was to create a readme file to specify what the project is. So, we're going to use Cloud Code to do that. We're going to open Cloud. And the first thing we should do before generating that Rhythmic that I specified is to start this project so that Cloud can understand it. Cloud Code uses commands, and commands are used with the slash. 

So, if we press the slash key and write it, we see that there are commands that are already available to use. And the first one we should use is init. Here it starts a cloud .md file with the documentation of the base code. So, let's hit enter. And here I'm going to do something important. It will analyze the entire database, it will understand what the project does, it will read all the files and in the end what it will do is write inside Cloud MD the instructions for Cloud to generate precise code on this database. So, here it has read everything. Notice that it works this way. It reads the files that are available. In this case, it read the pyproject .toml, which is where the dependencies of this service are identified. It reads the main .py file and so on with several files. 

Let's remember that in the documentation, Anthropic says that this is a coding tool based on agents. So the agent understands that the first thing he has to do before generating the code is to read. This is very interesting. Then what it says is that it will check if there is already a file clot .md within the codebase. And here then what it does is look. Look if there is a pattern that says clotmd, look if there is a readme .md, precisely what we need, look if there is something like that and find it. Here it says that it finds it. And here there is something important, it tells us that to expand what it found, we press Ctrl R, so let's see what it finds. Here it tells us that it found the things that are the Cursor Rules that we had defined in the previous video. and also found functions .mdc, which are the rules we describe for Cursor in that case. To close it, again it's Ctrl +R. 

Done, it closed. And then, at the end, it's that it writes the file. And this is the file that it's telling me it's creating. Let's check, because it was the security warnings it made. This file provides a guide to Cloud Code when it's working with this repository. Here it describes the commands that are available. This is to run the FAS API application and this is to run the application with Ubicron. It recognized that it's an FAS API application and gave us the two commands. to ensure the quality of the code and the tests, it makes these commands, they are perfect. Also the package management, perfect, and the architecture. This is a project of a API. What I'm seeing at a high level, I think it's correct. So, Note that Cloth will give us three options to choose from. 

One, accept immediately that this file is correct and write it. Two, that yes, I accept it and that it doesn't ask again when it generates a new code. Or three, tell it that there is something that is not right and that it should be different. In this case, to show how point three works, we are going to press three and here I am going to write what is not right with what it is saying. For example, here I am using black, but I don't want to use black. I know that in Python there is something called roof. So let's say that instead of the command to do the code formatting is with black, it does it with rough. So let's say, the instructions that can be given to Claude can be in English or in Spanish. In this case I'm going to do them in Spanish. He will also understand it and it will work correctly. Let's not use black for formatting the code. I want us to use rough. 

So, we're going to press enter and here it will start to do the analysis about what is wrong and how it will correct it. Let's see what it does. In this case, it is important because people have to realize that ROOF is not installed in this case. So, notice that the first thing it is going to ask me is that it is going to edit the pyproject .toml. So, I'm going to say yes. There it will add the rules of ROOF, in this case it changes Black for ROOF and adds ROOF as dependency. Great. So now it will create the QR code with the updated commands. This is very important. Why? Because I didn't want a simple description in a Markdown file, it understood that it also had to be installed in the system. That's too good. That's why it's an agent. And at the end, I'll see that the roof commands are already there. Perfect, I'll say yes. Great. And here what it will do is write them. Done. 

Since we are in the terminal, I'm going to exit, in this case, and I'm going to open the codebase with vim. So, we're going to open vim and we're going to list what we have here. So, we see that there is already a markdown file, that if we see it and contrast it with the repository, it didn't exist, but now it does, and it contains the rules that Cloud is going to understand to be able to generate efficient code on this codebase. When we have the instructions for Cloth, we can start using this with more tasks. So the first thing we're going to do is go to the Antropic documentation to see what are the CDI commands that we have available. Let's go there. In the Use CLI section, it will tell us how we can use Cloth as a command within the terminal, not only as the program I showed you where we interact chat -type. Cloth has several commands that allow us to do different things. 

And here is a list with the most important that we can do. So, notice that the first thing that goes, or the one we already use, is even the Cloth command that starts a REPL. And that's where we've been interacting for a while. But we can also use it this way, which is asking a question directly in the terminal. So, let's try how this works. I'm going to exit BIM and I'm going to say, Claude, I want to do an analysis on the OpenAPI .json of this service? How can I extract the JSON that defines FastAPI with the API that the project has? Maybe this is not the best prompt, but what do I want to do? 

I want Cloth, from a question I'm asking, to generate a command in which I can extract the JSON file from the OpenAPI. If we go back to the cursor video, we will see that the same FAS API will generate an interface of Swagger, which was the end of that video, where we saw the endpoints, what is the body and what is the description. So, I press enter and what will happen is that what I said here, in quotes, it uses it as the first input. Great. So, notice that here it is giving me a bash command. What it tells me is that the application will run on this port, 8888, and on the localhost. So, here it is asking me if I give it permission to execute a command inside my terminal. I'm going to say yes, to see what it generates. Done. Here we have an error. 

So let's see what it is correcting. It says there is an import error, so the code goes and it will correct it. I'm going to say yes. Let's see what it is doing, because it seems to be an import problem. I say yes again and wait. Look, in this case it's taking too long and it's because I think the command he chose to execute is not the correct one. So I'm going to hit cancel and here what's going to happen is that I'm going to exit the cursor interface. I can resume the conversations I already had with Claude from the flag C. So I can say Claude - C. And here what's going to happen is that it takes the previous conversation, I didn't lose the context and I'm going to say the following the command you were using is incorrect. 

Only with uvrun, uvcorn and the main file, without port specification, is enough to run the application. So let's see what it does. Here it should give me the command, I say yes. Since I already trust what it is generating, I will say yes, it doesn't ask me again to execute these commands within this project. I will say yes. Here it started running and what is happening is that the service is running here. If I go to my browser and I say localhost in port 8080, I think it is, I'm not sure. We'll stop this for a second and we'll ask it In which port does a FastAPI application run by default? 

Or better said In which port does this FastAPI app run by default? Let's see what it tells me. There it is reading the settings. Let's see what the port is. Ready. It's in port 8000. Execute the app. Let's see what it does. Ready. It tells me that there is already an application in running, of course, because I didn't stop the previous application. In this case, what it does is give me another port. So I'm going to copy it. 8889. So 8889. Done. This is the API that we already had. Now I'm going to ask you for one thing. and it will be using the command p. In this case, the command p allows me to use clot and get an immediate response. That is, the REPL will not open and it will give me the response within the terminal. 

So I'm going to ask it to give me the path in which I can get the OpenAPI .json file of this application. Here, what you'll have to do is read and analyze the project to understand where this address is. Let's see what it's doing. Here we can't see how it thinks, this is running in the background. But eventually, it will give me the answer. Great. So, here what it tells me is I have to enter the application and then go to the address /docs. So, let's see. Docs. and here it opens, excellent. And here I see that I have the link to openapi .json. Ready, I'm going to copy it, I'm going to open BIM and here what I'm going to do is make an openapi .json. And I'm going to copy what it had in the short form. 

In this case, we see that it is all the specification of the OpenAPI that it had in the browser. Great. I wanted to do this because I want to demonstrate a particularity that the CLI commands have. And that is that we can use the Operator Pipe. That is, this is based on the terminal purely. And that is that I can pass through the operator information from another command so that another command can do a job. So what are we going to do? I'm going to give it a cat a openapi .json. So if we see the openapi cat, it shows me the openapi specification that I already had. And I'm going to pass this to Claude through the OperatorPipe. This behavior is specified within the use of the CLI and here it explains it. What is it? Making a file cat and using the pipe, asking Claude a question. 

So we're going to do something similar and we're going to ask him which, or well, give me a list of the endpoints that this service has from openapi .json. As I'm asking for it with the -p flag, the REPL won't open, but it will give me the answer in the terminal. We'll wait for it to finish. Great. Based on the file, these are the endpoints that it says it has. The root, the health, get the list, of items, get an item, the dishes, etc. Which is what we had created with the cursor example. But in this case, Cloud did the analysis from the OpenAPI .json file. Very well. So, to explain more things about Cloud, what we're going to do is open the REPL and we're going to see the command list that is available here. 

For example, the first one is book, which is reporting to the Anthropic agent that there is a bug with the Cloud Code application. In this case, we're not going to use it. There is something important and it is the cost command. so, for example, during this whole session, it has not cost me anything. why? because cloud code is integrated or is allowed or is available with the Cloud Pro subscription that I already have bought. so it is not generating costs for me. if I had the second option, which is the one from the Anthropic console, there it will show me what is the cost that I have associated with the session that I am using. and shows it to me in dollars. Great. When I'm here, inside the command interface, besides the cost command, I have a doctor to know what the cloud code status is in this case, but it's not what I need to do, but also something important that we have inside the development flow is to choose the model that we have to use to generate code. 

In this case, Since I have the Pro subscription, which is not the most advanced one, I only have Sonnet available. If I had the Max subscription or the Cobra model with the Anthropic interface, Opus would be available here. that Opus is a slightly more advanced model than Sonnet. But Sonnet 4, currently, according to the benchmarks, is the best model we have available to generate code. So, if I had another model option available, I would select it here, but in this case, since I don't have another one, I chose Sonnet and it's perfect because it works pretty well. An important part that came out in the last release of Cloud Code is that it can be used directly within a text editor. Before this launch, Cloud Code was only available through the terminal. 

This meant that many times we couldn't correctly check what are the changes it's making or it was quite difficult to see them. now it's much easier. So what we're going to do is get out of Cloud Code and open Cursor, in this case with this project. We see that it's the same project we had. Here is the cloud .md that was the one that created it. And what we're going to ask in this case is to Cloud, open it inside the editor terminal. And there's a command called IDE. And this allows you to connect to the editor that is being used with Cloud Code that is running in the terminal. This is only available for VS Code and those VS Code forks like Cursor or like Windsurf and also with JetBrains editors. So if I say IDE, it will recognize that I have Cursor open, so I'm going to say yes. And here it will give me this little green circle, which means it's connected. 

So when I go to a file inside the editor, it will show me which file is selected down here. So, that's like I was giving context to Cloud Code about the file I'm working on. and like Cursor, I can mention files here and use them as context. Even select lines. How does this work? For example, we see that it is connected, but there is also the possibility of being able to use it here. So I'm going to click here, and I'm going to do it here. And what I have is the interface of the editor on the left side and Cloud Code on the right side. Great. We are going to make an edit here in the main .py. And we are going to tell it that instead of using Spanish recipes, we'll tell it to use Italian recipes. we'll say this here. how does this work? I select the lines and press, in this case, in mac, command option k. 

and here we see that on the right side where Cloud Code is, it selected the lines from 25 to 27 on the main .py file. and here I give it the instruction instead of using Spanish recipes, Please add, use Italian recipes and make a list of at least 10 recipes. Let's see. And here it starts, we see that it reads the file, reads which are the lines that I have selected and here it is doing its job. In this case, people are generating code. And unlike what happened in the terminal, I can see here what it generated and I can contrast it with what it had before. 

In this case, we see that these lines that are in red were eliminated, and added these lines that are in green and above me appear some options that is to reject the proposal or accept the proposal In this case, I'm going to accept what it generated. And here what it tells me, at the end, when I accept it, is that it's finishing its job. And it says it has replaced the Spanish recipe with 10 typical Italian recipes. One interesting thing that Cloud Code has are the commands. We had already mentioned them. But there is the possibility that I create commands to my measure. And it's quite easy to do. What we have to do is, in this case, is go where the files are, and we see that there is a folder called .cloud. I'm going to create a new folder called commands. And within this folder, I'm going to create a file. In this case, the commands are specified with the .md extension. 

That means that the commands are prompts that Cloud Code will understand and will execute. So in this case, I'm going to say python best practices .md. Done. And here what's going to happen is that this command is going to be available now inside this interface. I'm going to close it because it surely needs to restart. So we're going to finish and we're going to reopen it. And here we have Python best practices. That's a command that we already have available, but we see that there's nothing written here. So I'm going to use the same Cloth to be able generate content about the best practices of Python. In this case, something I would like is that it respects the type hints conventions that are now available in Python 3 .12 versus previous versions. So let's say it here. 

Help me build instructions in which we can, in which it is explained that conventions of Python 3 .12 in terms of the type hint. For example, in the generics and also in type. These instructions must be or will be used, rather, as commands that you will use yourself to format a Python file. You must be explicit and give rules and Python 3 .12 code styles. Let's see what it generates. 

Since this is connected to the editor, he has within his context that he must create these rules on this file. It's what I hope happens. So let's see if after he generates these rules, he will add them to this file. So notice that if he tried, but it says it hasn't been read yet. So the first thing it has to do is read it. And here it will read and it won't find content because it's empty. So here it generated it. Great, let's check what it specifies here. Done. Excellent, it uses the list instead of the list that was in previous versions. that the Pipe operator use to specify this type of typing instead of using the union. And that was exactly what I wanted it to do. So, let's say yes. Well, we can say yes here. Done. 

So, notice that we already have a command explained quite explicitly about the best practices of Python 3 .12. But how are they used? So, I'm going to go to the main file, I'll place myself on it, I certify that it's in the context of Cloud Code and I'll add the Python best practices command. And there it will grab the content of that file, it will use it to be able to format this Python file. In this case, I'll use the command and I'll tell it that it will be on the main .py file. Let's see. Great. Here it is reading the main file first and what I hope it does is that from the instructions it begins to improve the way I am writing the code here. So, look. Here's a problem with this. It's doing an update. 

Ah, something important. The same Cloud Code connects through the editor and can see the errors that are in the linter that the editor has. So, for example, if it breaks that a function receives a string and it's passing an integer, Cloud Code understands that there is an error and it will fix it by itself. So, look, it deleted a lot of things that weren't necessary anymore because in the prompt we had told it not to use optional but the operator pipe, not to use this list but the list with the lowercase l, it can be identified faster, and that in turn, the dict wouldn't use it either, like it's here. So, it deleted it and only left the annotated that was here. is writing down the return of each of the functions that are here. This seems impressive to me, to be honest. And what I do is hit accept. 

Here it will tell me CrowD Code that it has finished doing the analysis on main .py and improved it according to the conventions of Python 3 .2 and gives me a list of what he did. That is, quite simplified and summarized the list. But these were the actions he took on this file. Cloud Code is not only available to be used on the personal computer. Anthropic recently launched the possibility of Cloud Code being used inside a GitHub repository. This is quite easy to achieve. We won't do it in this class, but we'll see the steps to do it. So, let's go to the computer. In the Anthropic documentation, on the left side, we see the GitHub Actions section. In this case, it's specified how to install Cloth inside a GitHub repository. 

This will allow us to use Cloth to modify code, create code on any pull request or issue mentioning it. That is, I could create an issue in the repository explaining that I have certain problems with a feature and that it needs to be changed in a certain way. Claude will understand it, Claude will analyze the code base and will apply the changes to solve this issue. Here in the right section appear the configuration examples and here they will explain how it is that it can be installed inside the repository. The truth is that if you already have a continuous integration flow or continuous deployment with 10 -volt options, this will not be so different from what you already have with other workflows. Here we have an example directory where it shows how it should be done. 

The most classic, or the simplest and fastest way is to use this cloud .yaml. What it will do is, from a comment on an issue, a comment on a pull request, it will do the project checkout to bring it and, in turn, execute the cloud command. Cloud Code seems extremely powerful to me. Within the Platzi engineering team, we use it. just like cursor. but cloud code allows us to execute much bigger and much more complex tasks without a time limit or executions on a task. that's why for me Cloud Code makes a lot more sense when what needs to be done involves a lot of steps or a lot of time to be able to execute. You could also run Cloud Code in multiple windows of your terminal. 

That is, you will have multiple agents running, maybe doing the same task to then do a kind of race to see who did the best solution, or that each one is developing a feature on different databases. For me, this is not a cursor competition, they are complementary tools. So, just like Cursor, Cloud Code can be used to generate code through artificial intelligence. Very soon, in Platzi, we will have the Cursor course available so that you can learn how to use artificial intelligence to develop code. 

