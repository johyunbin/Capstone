# handoff 20260519 06:54 — 엔진 적용 검증 ultraplanning 완료 + 측정 harness 3종 골격

> 이전 handoff(`handoff_20260519_000226_세션2_deck슬리밍완료`, 세션 2 완료) → 본 문서. 이 문서 하나만 읽으면 0% loss 인계 — self-contained.
>
> **핵심 한 줄**: 세션 3 = ① 5/27 발표 deck 최종본(`속도는벡터_최종발표_슬라이드.pptx` 19장)을 전수 검증 — 통과, 5/26 LearnUs 제출만 남음(사용자 작업). ② **"엔진 적용 검증" 실험 ultraplanning 완료·사용자 승인** — 오프라인에서 검증된 카디널리티 추정치를 pg_hint_plan `Rows()` 힌트로 실제 PostgreSQL 실행 계획에 주입해 "추정치 → 실행 계획 → 실제 latency"를 4조건으로 검증하는 실험. ③ 새벽 스코프로 측정 harness 3종 골격을 작성하고 로컬 스모크 테스트 통과. ⚠️ **연구실이 5/19 아침 서버 PostgreSQL 사용 자제를 요청 → Phase 0부터의 서버 작업은 5/19 아침 사용 허가를 받은 뒤 착수.** 다음 세션 = 허가 확인 → Phase 0(scp·ANALYZE·플랜 민감도 트리아지) 실행.

---

## 0. 가장 먼저 — 정본 문서

- **★ 승인된 ultraplan**: `~/.claude/plans/swift-discovering-engelbart.md` — 엔진 적용 검증 실험 6-Phase 계획. **다음 세션의 실행 지침서.**
- **측정 harness 3종** (`_internal/scripts/`): `measure_latency_realengine.py` · `gen_latency_estimates.py` · `analyze_latency.py` — 골격 완성, 로컬 스모크 통과, 서버 미실행.
- **deck 최종본**: `submission/_drafts/속도는벡터_최종발표_슬라이드.{pptx,pdf}` — 검증 통과.
- **수치 정본**: `_internal/cache/rq3/v13_summary.md`. **제출 공지**: `_internal/state/캡스톤_제출공지_20260518.md`. **narrative**: `submission/_drafts/속도는벡터_본연구_narrative_20260518_175437.md`.

## 1. 본 연구 framing (불변 — 가장 먼저 내재화)

본 연구는 Exqutor 논문(arXiv:2512.09695v2) §V-B Adaptive Sampling 재현이 아니다. cardinality 추정 파이프라인에서 **sample selection 단계 하나**의 개입(무작위 Bernoulli → 분포 인지 stratification)이 추정 오차(Q-error)에 미치는 영향을 전 데이터셋·전 조작 변인에 걸쳐 검증한 완전 실험이다. 측정은 **3-way matched** — B1(대조군·논문 그대로 무작위) / CaseA(완전 대체) / CaseB(결합, est=(est_b1+est_method)/2). AdaptiveState 식 1-6은 논문 그대로.

**이번 task(엔진 적용 검증)** = 그 오프라인 실험이 닫지 않은 고리 — "더 정확한 추정 → 더 나은 실행 계획 → 더 빠른 쿼리" — 를 실제 PostgreSQL 엔진에서 end-to-end로 검증. 박광현 교수 권고 + 5/27 deck slide 18이 "다음 단계"로 약속. 결과는 6/11 보고서 반영.

★ 발표·자문·포스터 자료에서는 B1/CaseA/CaseB 코드명을 쓰지 않고 "기존 방식 / 완전 대체 / 결합"으로 풀어쓴다.

## 2. 이 세션(5/19 01:00~06:54)이 한 일

| 항목 | 상태 | 내용 |
|---|---|---|
| deck 최종본 검증 | ✅ | `속도는벡터_최종발표_슬라이드.pdf` 19장 전수 시각 검증 — slide 9 라벨 잘림 수정 확인, slide 13 막대 v13 median Δ% 일치, 파일명 공지 양식 일치. **통과 — 5/26 제출 준비 완료** |
| 엔진 검증 — 5축 조사 | ✅ | 오프라인 파이프라인 / Exqutor §V 방법론 / 서버 상태 / 실현가능성 / VAQ SQL 구조 — 병렬 에이전트 5회. §6 참조 |
| 엔진 검증 ultraplanning | ✅ | AskUserQuestion 2건 → 방법론·harness 독립 검토 2건 → 6-Phase plan 작성 → **ExitPlanMode 승인** |
| harness 3종 골격 작성 | ✅ | `measure_latency_realengine.py`·`gen_latency_estimates.py`·`analyze_latency.py` — §7 |
| 로컬 스모크 테스트 | ✅ | dry-run(SQL 변환·힌트 주입 3종) + analyze self-test + py_compile — **전부 통과**(서버 미접속) |

## 3. ★ 엔진 적용 검증 실험 설계 (정본 — `swift-discovering-engelbart.md` 요약)

**본질**: 엔진을 고정하고 카디널리티 추정치만 유일 변인. 같은 VAQ 쿼리·같은 PostgreSQL에서 벡터 술어의 base-relation 카디널리티에 주는 추정치만 4조건으로 바꿔 실행 계획·latency 비교.

| 조건 | 주입 | 의미 |
|---|---|---|
| 기본 엔진 | 힌트 없음 | 플래너 default selectivity (개입 없음) |
| 베이스라인 | `Rows(<벡터테이블> #est_B1)` | 논문 무작위 Bernoulli 추정 |
| 결합 | `Rows(<벡터테이블> #est_CaseB)` | 결합 추정 = (est_b1+est_method)/2 |
| 오라클 | `Rows(<벡터테이블> #true_card)` | 참 카디널리티 — 이론적 상한 |

측정 사슬 = Q-error(오프라인 기검증) → 실행 계획 변화 → end-to-end latency.

**범위**(사용자 선택): pg_hint_plan 주입 우선 + 여유 시 Exqutor C 패치 live 통합(Phase 4 stretch). PostgreSQL 깊이 — 다중 method(가중 표본 추출·Hilbert·PCA·HyperLogLog)·다중 데이터셋(DEEP·SIFT·SSN)·다중 VAQ 쿼리. **DuckDB는 제외**(미설치+주입 인터페이스 부재) → 6/11 보고서 future work.

**Phase**: 0 셋업+플랜 민감도 트리아지(go/no-go 게이트) → 1 harness+추정치 생성 → 2 전 매트릭스 → 3 분석·figure → 4(stretch) C-패치 live → 5 보고서 반영.

**방법론 통제**(독립 검토 반영): 조건 순서 매 반복 무작위 셔플(캐시 교란), ANALYZE 통계 고정, statement_timeout 건은 censored 별도, 조건별 새 세션+force_custom_plan, HNSW 인덱스 경로 확인, 1 warmup+15 timed, paired Wilcoxon.

## 4. 다음 세션 task

1. **★ 서버 PostgreSQL 사용 허가 확인** — 연구실이 5/19 아침 사용 자제 요청. 사용자에게 허가 여부를 먼저 확인. **허가 전엔 서버 작업 일절 금지.**
2. **Phase 0** (허가 후) — ① harness 3종 + VAQ SQL(`reference/exqutor_query_plans/`)을 서버 `/mnt/hdd0/home/capstone2026/cache/rq3/`에 scp ② 관련 테이블 ANALYZE ③ 플랜 민감도 트리아지: 후보 쿼리×데이터셋에 조건 ①기본·④오라클만 실행, EXPLAIN 비교 → 플랜 갈리는 쿼리=코어 / 안 갈리는 쿼리=보조지표 트랙 ④ go/no-go 게이트.
3. **Phase 1** — `gen_latency_estimates.py` 실행해 추정치 parquet 생성 → `measure_latency_realengine.py`로 1 cell 실측 검증.
4. **Phase 2~3** — 전 매트릭스 실행 + 분석·figure. (Phase 4 stretch, Phase 5 = 6/11 보고서.)
5. (deck) 사용자가 `속도는벡터_최종발표_슬라이드.pptx`를 5/26 23:59 LearnUs 제출. Claude 작업 없음 — 검증 완료.

각 Phase는 비자명 → 필요 시 ultraplanning. 단 plan이 이미 승인됐으므로 plan대로 실행하되 Phase 0 게이트 결과에 따라 재계획.

## 5. 산출물 경로

| 항목 | 경로 | 상태 |
|---|---|---|
| 승인된 ultraplan | `~/.claude/plans/swift-discovering-engelbart.md` | 승인 완료 · 실행 지침서 |
| latency harness | `_internal/scripts/measure_latency_realengine.py` | 골격 완성 · dry-run 통과 |
| 추정치 생성기 | `_internal/scripts/gen_latency_estimates.py` | 골격 완성 · 서버 전용 |
| 분석 스크립트 | `_internal/scripts/analyze_latency.py` | 골격 완성 · self-test 통과 |
| harness 산출물 dir | `_internal/cache/rq3/latency/` (예정) | 미생성 |
| deck 최종본 | `submission/_drafts/속도는벡터_최종발표_슬라이드.{pptx,pdf}` | 검증 통과 · 5/26 제출 대기 |
| VAQ SQL 15개 | `reference/exqutor_query_plans/{tpc_h,tpc_ds}/q*.sql` | 로컬 보유 · 서버 scp 필요 |

## 6. 엔진 검증 — 조사로 확정된 사실 (서버·코드·실현가능성)

새 세션이 재조사 없이 바로 쓸 수 있도록 정리 — 5축 조사 결과.

**서버** (165.132.140.240, capstone2026, `ssh capstone`, 작업 dir `/mnt/hdd0/home/capstone2026/`):
- 포트 **55435 = Exqutor 패치 PostgreSQL 16.9** (바이너리 `/mnt/ssd_238/Exqutor/PostgreSQL/pgvector/psql/bin/postgres`, data dir 이름만 `vanilla_sf100`). Exqutor hook 비활성(`SHOW vector.sample_size` 실패) = **깨끗한 바닐라 동작 → "기본 엔진" 대조군으로 그대로 사용**. DB `wns41559`, user/pw `wns41559`/`wns41559`. 55436 가용.
- **pg_hint_plan 1.6.2** 빌드 완료. 세션마다 `LOAD 'pg_hint_plan'`로 활성(superuser·preload 불필요). `Rows(테이블 #N)` 힌트로 base-rel 카디널리티 주입 → 비용 재계산 실측 확인.
- 벡터 테이블 `partsupp_{deep,sift,fb}_{1,10,100}` 등 16개(deep_10=8M행, deep_100=80M행), **HNSW 인덱스 18개**(m=16, ef_construction=200), `ps_embedding`+`stratum_id` 컬럼. `vector_stratum_sigma` 테이블(table_name·stratum_id·sigma 300행) 적재됨.
- 오프라인 3-way 측정 완료 — `cache/rq3/paper_exact/` B1 9 + CaseA 495 + CaseB 496 = 1000 JSON. 디스크 13T 중 1.3T 여유.
- **DuckDB 미설치** — 바이너리·패키지 0, Exqutor DuckDB 서브모듈 빈 디렉토리, 외부 카디널리티 주입 인터페이스 부재. → 본 실험 제외, future work.
- **Exqutor C 패치**(Phase 4 stretch): `Exqutor/.../pgvector_Exqutor.patch`의 `vector.c` `estimate_cardinality_with_sampling` — 무작위 추출이 `TABLESAMPLE SYSTEM(%)` SQL 한 줄. stratum 기반 교체 ~10줄. 빌드 `apply_patch.sh`→`build_custom.sh`(128코어 수 분).

**VAQ 쿼리**: `reference/exqutor_query_plans/`에 15개 — TPC-H 8(q3·5·8·9·10·11·12·20, `partsupp_deep`·`ps_embedding`·threshold 0.86) + TPC-DS 7(q07·12·19·20·42·72·98, `item_deep AS i`·`i_embedding`·threshold 1.08/1.20/1.30). 각 쿼리 = 표준 TPC 쿼리 + 벡터 술어 `<-> 'image_embedding' < <thr>` + ORDER BY. `'image_embedding'`은 placeholder. join 풍부(오추정 영향 큼) = ds-q72(11)·th-q8(9)·th-q5(7)·ds-q19(6). 서버엔 미 scp.

**추정치↔VAQ 매핑**: 벡터 술어 카디널리티 = 벡터 테이블 base-relation 카디널리티 → `Rows(<벡터테이블> #N)` 주입. 오프라인 추정기와 같은 L2 거리. 거리 임계값 D = 목표 selectivity(0.001/0.01/0.10)별 `D_target`(`query_selectivity_*.parquet`), 짝 `true_cardinality`가 참값.

**재사용 코드** (수정 안 함): `_internal/scripts/measure_paper_exact.py`(`measure_b1_paper:361`, `measure_case_b:1087` — CaseB 결합 `:1148-1153`, `measure_ecqo` EXPLAIN ANALYZE 패턴 `:1223-1243`, `_get_method_strata:483`, `q_error:153`, `trimmed_mean:160`, `DATASET_ALIAS:56`), `_internal/scripts/_measure_common.py`(`bernoulli_estimate:354`, `stratified_estimate:337`, `_load_query_pool:373`, `fetch_all_vectors_safe:155`, `equal_alloc:237`). ⚠️ **`_measure_common.py:52` `PORT=55436`은 stale — 실제 55435** (measure_paper_exact.py가 `mc.PORT=55435`로 override; harness 3종도 55435 사용).

## 7. 측정 harness 3종 — 골격 상태

전부 `_internal/scripts/`. 골격 완성·로컬 스모크 통과·서버 미실행. 서버에서 `_measure_common.py`와 같은 dir(`cache/rq3/`)에 두고 실행.

- **`measure_latency_realengine.py`** — harness. VAQ SQL 로드 → `partsupp_deep`→실제 테이블 치환 + `'image_embedding'`→qvec 리터럴 + threshold→D 치환 + `EXPLAIN(ANALYZE,BUFFERS,FORMAT JSON)`+`/*+ Rows() */` 힌트. 매 반복 variant 순서 무작위 셔플, 조건별 새 세션, 1 warmup+15 timed, statement_timeout censored. `--dry-run` = 서버 미접속 SQL 변환 검증(통과). real run = `--estimates <parquet>` 필요.
- **`gen_latency_estimates.py`** — 추정치 생성(서버 전용 — 벡터 fetch에 PG 필요). `measure_b1_paper`/`measure_case_b` 로직을 단일 query·고정 budget(385)으로 축소. 출력 parquet `(dataset,sf,sel,query_id,method,est_b1,est_caseA,est_caseB,true_card,D,qvec)`.
- **`analyze_latency.py`** — 분석. harness 산출 JSON → latency 비교표·speedup·플랜 변화 카탈로그·Q-error↔latency. `--self-test`(합성 데이터) 통과.

## 8. 핵심 수치 — v13 정본 (`v13_summary.md` — 불변)

- 측정 **1508건** 3-way matched. **CaseB(결합) vs B1 better 89.1%**(1344/1508) · 중앙값 Δ% −4.38% · 유의 65.3% · δlarge 72.1%.
- CaseA(완전 대체) vs B1 better 35.2%. CaseB가 완전 대체를 **96.5%** 우월.
- method 정확도 **median Δ% 1위 = 가중 표본 추출(chao_weighted) −6.22**. **강한 method 13개 / 클러스터링 계열 3개 제외**(gmm·minibatch_partial·faiss_ivf).
- selectivity 0.001/0.01/0.10 → better **83.3 / 87.6 / 97.5%**. K granularity 8-cell K 10/20/30 → **83.6 / 89.8 / 85.9%**(K=20 최강).
- 데이터셋 **9종**(단일 5 + 다중 4, 96~1024차원). 단일 89.1% / 다중 89.2%. better% = 측정별 평균 Δ%<0 비율(mean 기준).

## 9. 사용자 복귀 시 확인 / 미해결

- **★ 서버 PostgreSQL 사용 허가** — 연구실 5/19 아침 자제 요청. 다음 세션 시작 시 허가 여부 확인 필수.
- **커밋 미실행** — 이번 세션 산출물(harness 3종·handoff·plan은 ~/.claude). deck 파일(`속도는벡터_최종발표_슬라이드.*`)도 미커밋. push·commit은 사용자 명시 요청 시만.
- **deck** — 검증 통과. 사용자가 5/26 23:59 LearnUs에 `속도는벡터_최종발표_슬라이드.pptx` 제출(PDF 불가). Claude 잔여 작업 없음. `deck_v17.pptx`(슬리밍 적용본·구버전)는 `_drafts/`에 잔존 — 정리 시 archive 가능.
- **plan 파일 위치** — `~/.claude/plans/swift-discovering-engelbart.md`(plan mode 자동 명명). 타임코드 네이밍은 아니나 승인된 정본.
- CLAUDE.md "동적 state" anchor stale(handoff_v37) — 미해결, 디렉토리 정리 작업에서 갱신 예정.

## 환각 회피 룰 (필독)

- 수치는 `v13_summary.md` 실측. method 순위 = **median Δ% 기준**(1위 가중 표본 추출 −6.22).
- 강한 method **13개**, 클러스터링 계열 **3개 제외**(gmm·minibatch_partial·faiss_ivf). "14강/2제외" 금지.
- 데이터셋 **9종**(96~1024차원). K granularity = 8-cell, K 10/20/30 → 83.6/89.8/85.9%.
- better% = 측정별 평균 Δ%<0 비율(mean 기준).
- 서버 PORT = **55435**(`_measure_common.py`의 55436은 stale). DuckDB는 본 실험서 제외(미설치).
- 발표·자문·포스터 자료엔 코드명(B1/CaseA/CaseB)·수식·"분포 안다/모른다" 이분법 금지. 필러 토큰("영역" 등) 반복 금지.

---

작성: 2026-05-19 06:54 KST · 세션 3 완료 → 다음 = 서버 사용 허가 확인 → 엔진 적용 검증 Phase 0(scp·ANALYZE·플랜 민감도 트리아지).
