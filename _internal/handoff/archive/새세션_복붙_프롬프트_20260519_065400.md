# 캡스톤 새 세션 — 엔진 적용 검증 Phase 0 (서버 작업)

> 정책: 한국어 · peer-to-peer · Opus 최신 · 전권 위임 · 자원/토큰 Max · 병렬 에이전트도 Opus · 도구·커넥터·스킬 선제 로드 · 비자명 작업은 ultraplanning → 승인 → 실행.

## 0. 먼저 — 인계 정독
@_internal/handoff/active/handoff_20260519_065400_엔진검증_harness골격.md — 직전 세션(세션 3) 인계, self-contained.
승인된 실행 계획: @~/.claude/plans/swift-discovering-engelbart.md
수치 정본: @_internal/cache/rq3/v13_summary.md

## 1. framing
Exqutor 논문 재현 아님. sample selection 단계 개입(무작위 → 분포 인지 stratification)이 cardinality 추정 Q-error에 미치는 영향을 전 변인에서 검증한 완전 실험 — 3-way matched(기존 방식/완전 대체/결합). 이번 task = 그 검증된 추정치를 실제 PostgreSQL에 주입해 "추정치 → 실행 계획 → 실제 latency"로 닫는 엔진 적용 검증. 발표·보고서 자료엔 코드명 금지.

## 2. 직전 세션
세션 3 = ① 5/27 발표 deck 최종본 검증 통과(5/26 제출만 남음) ② 엔진 적용 검증 ultraplanning 완료·승인 ③ 측정 harness 3종(`measure_latency_realengine.py`·`gen_latency_estimates.py`·`analyze_latency.py`) 골격 작성·로컬 스모크 통과.

## 3. 이번 세션 — 엔진 검증 Phase 0 (서버)
⚠️ **먼저 연구실 서버 PostgreSQL 사용 허가를 사용자에게 확인.** 5/19 아침 자제 요청이 있었음 — 허가 전엔 서버 작업 일절 금지.

허가 후, 승인된 plan(`swift-discovering-engelbart.md`)의 Phase 0:
1. harness 3종 + VAQ SQL(`reference/exqutor_query_plans/`)을 서버 `/mnt/hdd0/home/capstone2026/cache/rq3/`에 scp
2. 관련 테이블 ANALYZE (TPC-H base + 벡터 테이블) — 상태 고정
3. 플랜 민감도 트리아지 — 후보 쿼리×데이터셋에 조건 ①기본·④오라클만 실행, EXPLAIN 비교 → 플랜 갈리는 쿼리=코어 / 안 갈리는 쿼리=보조지표 트랙
4. go/no-go 게이트 — 오라클에서도 플랜 불변이면 멈추고 보고
이어 Phase 1(추정치 생성 + harness 1 cell 검증) → Phase 2(전 매트릭스) → Phase 3(분석).

## 4. 작업 방식·정책
서버 작업은 read-only 우선, 변경(ANALYZE 등)은 사용자 인지하에. statement_timeout·연결 수 제한으로 공유 서버 예의. 비가역 작업은 사전 승인. 산출물 타임코드. 커밋·push는 명시 요청 시만. 환각 회피: 수치 v13 실측·코드명 금지·PORT 55435·DuckDB 제외.

## 5. 시작
handoff 정독 → 서버 PostgreSQL 사용 허가 확인 → 허가 시 Phase 0 착수.
