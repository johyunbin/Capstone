# 캡스톤 새 세션 — 엔진 적용 검증 Phase 0 (plan 민감도 prescan)

> 정책: 한국어 · peer-to-peer · Opus 최신 · 전권 위임 · 자원/토큰 Max · 병렬 에이전트도 Opus · 도구·커넥터·스킬 선제 로드 · 비자명 작업은 ultraplanning → ExitPlanMode 승인 → 실행.

## 0. 먼저 — 인계 정독
@_internal/handoff/active/handoff_20260520_021334_harness개편.md — 직전 세션 인계, self-contained 0% loss. 이 문서 하나로 완전 인계. 라우팅·구조 정본은 루트 CLAUDE.md, 승인 plan 은 `~/.claude/plans/imperative-crafting-tome.md`.

## 1. framing
본 연구는 Exqutor 논문 재현이 아니라 표본 선택 단계 개입(무작위 Bernoulli → 분포 인지 stratification)의 추정 오차 효과를 검증한 완전 실험(3-way B1/CaseA/CaseB). 「엔진 적용 검증」 = 오프라인 검증된 카디널리티 추정치를 실 PostgreSQL(서버 55435)에 주입해 "추정치 → 실행 계획 → end-to-end latency" 고리를 닫는 실험. 4조건: 기본엔진 / 베이스라인(est_B1) / 결합(est_CaseB) / 오라클(true_card).

## 2. 직전 세션이 한 일 (Phase 1 — harness 개편)
서버 recon 으로 직전 handoff 의 "주입 검증 완료"가 불완전함을 발견·정정: ① 주입은 벡터 테이블이 pass-1 에서 SeqScan 될 때만 발동(IndexScan 경로 skip) ② 실행 시점 2-pass — 순수 EXPLAIN 무효 ③ plan capture 는 auto_explain ④ 세션마다 `LOAD 'vector'` ⑤ 테이블 `<name>_<sf>` → 임시 VIEW. 정정된 메커니즘으로 harness 3종 전면 개편·서버 probe 실증 완료(q3·DEEP·sf10: baseline 8.3초 → 주입 2.4초, 2.6~3.5×, 플랜 Hash Join → Nested Loop+Index Scan 전환). harness 확정.

## 3. 이번 세션 작업 — Phase 0 plan 민감도 prescan (최우선)
handoff §5 참조. ① 8 TPC-H 쿼리 × DEEP × sf10 × sel{0.001,0.01,0.1}에 기본·오라클 조건만 빠르게 측정 → 각 cell 의 injection_fired·플랜 변화 확인. ② 분류: injection_fired=True & 오라클 플랜 ≠ baseline 플랜 = 코어 매트릭스 / 그 외 = 보조·제외. ③ go/no-go 게이트 — 코어 cell 충분하면 Phase 2, 거의 없으면 정지·보고. 이후 → Phase 2 매트릭스(결합 ×13 method).

## 4. 작업 방식·주의
- harness 3종은 서버 `/mnt/hdd0/home/capstone2026/cache/rq3/` 에 배포됨 — `python3 measure_latency_realengine.py --help` 로 인터페이스 확인. `ssh capstone` / `PGPASSWORD=wns41559 psql -h localhost -p 55435 -U wns41559 -d wns41559`.
- 측정마다 `injection_fired` 확인 필수 — false 면 그 측정은 기본 selectivity 플랜이라 무효.
- latency = 실쿼리 직접 timing(EXPLAIN ANALYZE 금지). 플랜 = auto_explain. 메커니즘 정본 = handoff §4.
- vector.c·`build_custom.sh`/`apply_patch.sh` 손대지 말 것. 작업 포트 55435만.
- 비자명 작업은 ultraplanning → 승인 후 실행.

## 5. 시작
handoff 정독 → harness 인터페이스 확인 → Phase 0 prescan ultraplan → 승인 → 실행.
