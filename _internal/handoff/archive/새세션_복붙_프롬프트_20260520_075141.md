# 캡스톤 새 세션 — 엔진 적용 검증 Phase 2 (축소 매트릭스 측정)

> 정책: 한국어 · peer-to-peer · Opus 최신 · 전권 위임 · 자원/토큰 Max · 병렬 에이전트도 Opus · 도구·커넥터·스킬 선제 로드 · 비자명 작업은 ultraplanning → ExitPlanMode 승인 → 실행.

## 0. 먼저 — 인계 정독
@_internal/handoff/active/handoff_20260520_075141_phase0prescan완료.md — 직전 세션 인계, self-contained 0% loss. 이 문서 하나로 완전 인계. 라우팅·구조 정본은 루트 CLAUDE.md. 승인 Phase 0 plan 은 `~/.claude/plans/sorted-twirling-cloud.md`.

## 1. framing
본 연구는 Exqutor 논문 재현이 아니라 표본 선택 단계 개입(무작위 Bernoulli → 분포 인지 stratification)의 추정 오차 효과를 검증한 완전 실험(3-way B1/CaseA/CaseB). 「엔진 적용 검증」 = 오프라인 검증된 카디널리티 추정치를 실 PostgreSQL(서버 55435)에 주입해 "추정치 → 실행 계획 → end-to-end latency" 고리를 닫는 실험. 4조건: 기본엔진 / 베이스라인(est_B1) / 결합(est_CaseB ×13) / 오라클(true_card).

## 2. 직전 세션 결과 — Phase 0 prescan
24 cell(8 TPC-H × DEEP × sf10 × 3 sel × qid=0) 분류:
- **core 4** = q3·q9·q10·q12 × **sel=0.001** ★ — base sig ≠ B1 sig ≠ oracle sig 모두 다름. Phase 2 1순위.
- plan-saturated 5 — 같은 4 쿼리 × sel=0.01 (+ q9 sel=0.1). B1 만으로 oracle 플랜 도달.
- plan-invariant 3 — sel=0.1 q3/q10/q12. 플래너 둔감.
- **injection-MISS 12** — q5·q8·q11·q20 × 전 sel. partsupp pkey IndexScan 경로 → 주입 skip. Phase 2 측정 무의미 → 보고서 honest limitation 반영.

**결정**: GO 축소 Phase 2 (core 4 cell 만). **핵심 패턴**: 쿼리 8개 중 4가 SeqScan, sel↓=plan 민감도↑.

## 3. 이번 세션 작업 — Phase 2 축소 매트릭스 측정 (최우선)
handoff §5 참조.

1. **core 4 cell 측정** (≈1-1.5시간): q3/q9/q10/q12 × DEEP × sf10 × sel=0.001 × qid=0. variant = baseline/B1/oracle + CaseB×13 = 16. n_timed=15, warmup=1, timeout=600s. harness `measure_latency_realengine.py` 사용.
   ```
   ssh capstone "cd /mnt/hdd0/home/capstone2026/cache/rq3 && \
     for q in q3 q9 q10 q12; do \
       python3 measure_latency_realengine.py --query \$q --dataset DEEP --sf 10 \
         --sel 0.001 --query-id 0 --estimates latency/estimates_DEEP_sf10.parquet \
         --output latency/phase2 --n-timed 15; \
     done"
   ```
2. **회수·분석**: `analyze_latency.py --input latency/phase2 --output latency/phase2/figures` → 4 cell × 16 variant 의 trimmed latency, plan 변화, Q-error↔latency. 서버 matplotlib 미설치 → figure 는 로컬에서 생성.
3. **(권장) qid 1·2 확장**: core 4 cell × {qid 1, qid 2} (≈30분-1시간). plan 변화 패턴 안정성 확인.
4. **(권장) saturated 5 cell 의 latency 측정**: plan 변화 없지만 latency 분산은 별개. 후순위.
5. **honest limitation 정리**: MISS 12 cell 카탈로그 (Exqutor SeqScan-only 제약) → 6/11 보고서·발표 반영.

**일정 압박**: 5/26 PPTX 마감·5/27 발표·5/28 포스터. Phase 2 결과를 5/25 까지 반영해야 deck/포스터에 들어감.

## 4. 작업 방식·주의
- harness 3종은 서버 `/mnt/hdd0/home/capstone2026/cache/rq3/` 에 배포됨. SSH: `ssh capstone`. PG: `PGPASSWORD=wns41559 psql -h localhost -p 55435 -U wns41559 -d wns41559`.
- 측정마다 `injection_fired` 확인 필수. core 4 cell 은 prescan 에서 fired=T 검증됨 — Phase 2 도 fired=T 여야 정상.
- latency = 실쿼리 직접 timing (EXPLAIN ANALYZE 금지). 플랜 = auto_explain. 메커니즘 정본 = handoff §2.
- vector.c·`build_custom.sh`/`apply_patch.sh` 손대지 말 것. 작업 포트 55435만.
- 비자명 작업은 ultraplanning → ExitPlanMode → 실행.
- **핵심 hallucination 회피**: probe q3 sel0.01(2.6~3.5×)는 plan-saturated cell — B1 만으로도 일어난 단축. "CaseB > B1" 주장 generalize 금지. core 4 cell 의 측정으로만 검증.

## 5. 시작
handoff 정독 → Phase 2 측정 plan ultraplan → 승인 → 실행 → 회수·분석 → handoff 인계.
