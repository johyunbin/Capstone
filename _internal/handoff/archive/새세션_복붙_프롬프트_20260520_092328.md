# 캡스톤 새 세션 — Phase 2 결과 반영 (6/11 보고서 + 5/26 deck + 5/28 포스터)

> 정책: 한국어 · peer-to-peer · Opus 최신 · 전권 위임 · 자원/토큰 Max · 병렬 에이전트도 Opus · 도구·커넥터·스킬 선제 로드 · 비자명 작업은 ultraplanning → ExitPlanMode 승인 → 실행.

## 0. 먼저 — 인계 정독
@_internal/handoff/active/handoff_20260520_092328_phase2완료.md — 직전 세션 인계, self-contained 0% loss. 이 문서 하나로 완전 인계. 라우팅·구조 정본은 루트 CLAUDE.md. 승인 Phase 2 plan 은 `~/.claude/plans/lazy-roaming-hanrahan.md`.

## 1. framing
본 연구는 Exqutor 논문 재현이 아니라 표본 선택 단계 개입(무작위 Bernoulli → 분포 인지 stratification)의 추정 오차 효과를 검증한 완전 실험(3-way B1/CaseA/CaseB). 「엔진 적용 검증」 = 오프라인 검증된 카디널리티 추정치를 실 PostgreSQL(서버 55435)에 주입해 "추정치 → 실행 계획 → end-to-end latency" 고리를 닫는 실험. 4조건: 기본엔진(baseline) / 베이스라인(est_B1) / 결합(est_CaseB ×13) / 오라클(true_card).

## 2. 직전 세션 결과 — Phase 2 완료 (12 cell × 16 variant × 15 rep)

**5대 finding** (정본 §4):
1. **모든 12 cell 에서 baseline 대비 plan 변화 + 3-7× speedup** (평균 5.67×, qid 무관 robust)
2. **B1 plan = oracle plan: 7/12 cell** — qid 의존 fragile (qid=0 0/4, qid=1 4/4, qid=2 3/4)
3. **CaseB ×13 oracle-align: 148/156 = 94.9%** — qid 무관 robust → **CaseB > B1 의 plan 회복 robustness 측면 가치 명확**
4. **paired Wilcoxon**: baseline-vs-* **180/180 (100%) 유의** / B1-vs-* **13/168 (7.7%) 유의** — latency 차이 통계 검증 (대부분 cell 에서 B1 ≈ CaseB ≈ oracle latency)
5. q3 만 정확도 미달 method (qid=0=hilbert_real, qid=1=sparse_rp, qid=2=6 method split) — 다른 3 쿼리는 13/13 align

**산출물 정본**: `_internal/cache/rq3/latency/phase2/` (12 JSON + 2 log + analyze_stdout_full.txt + `figures/paired_stats.csv` + 13 PNG). 서버 mirror 동일.

**핵심 수치** (q3/qid0 예):
- baseline 7242ms → oracle 1000ms = **7.24× speedup**
- B1 1018ms (est=1.0 클램프) — sub-optimal Nested Loop chain, oracle ≠ B1 plan
- CaseB ×13: 12/13 oracle plan 회복 (hilbert_real 만 Hash Join 잔존)

**해결된 의문**: prescan 의 q3 B1 plan_dur 70.7s = EXPLAIN cold cache + compile 비용 → 실측 15-rep mean 1018ms. 측정 정상.

## 3. 이번 세션 작업 — 6/11 보고서·5/26 deck·5/28 포스터 반영 (최우선)
handoff §5 참조.

1. **[최우선] 6/11 보고서 §4 (RQ3 결과) 에 Phase 2 통합** (5/25 backstop):
   - `submission/_drafts/속도는벡터_6_11_최종보고서_20260519_135021.md` 의 §4 (또는 신규 §) 에 §4.1 (12 cell latency 매트릭스) + §4.2 (plan 회복 매트릭스 — B1=7/12 fragile vs CaseB=94.9% robust) + §4.3 (Wilcoxon 통계 검증) + §4.4 (honest 한계) 추가.
   - injection-MISS 12 cell (q5/q8/q11/q20 × 전 sel) honest limitation 카탈로그.
2. **[필수] 5/26 deck·5/28 포스터·소개영상**:
   - `submission/_drafts/속도는벡터_최종발표_슬라이드_20260519_223845.pptx` — RQ3 슬라이드(들)에 Phase 2 매트릭스·plan 회복 패턴·speedup 그래프 반영.
   - 발표물 4종(deck·포스터·팜플렛·소개영상) 동일 메시지.
   - claude.ai/design 프롬프트 재생성 — 새 figure 12 PNG + paired_stats.csv 가공.
3. **[권장] saturated 5 + invariant 3 cell 측정** (Phase 0 carry-over) — 약 1시간. 보고서 §honest limitation 강화.
4. **[권장] 통계 검증 보강** — paired_stats 에 Cohen d / Cliff δ / bootstrap CI 추가.

**일정 압박**: 5/26 PPTX 마감·5/27 발표·5/28 포스터·6/11 보고서.

## 4. 작업 방식·주의
- handoff §4 의 5대 finding + §9 환각 회피 룰 흡수 후 작업.
- 측정값·plan 매트릭스 정본 = `paired_stats.csv` + handoff §4.1/§4.2 표. 다른 수치 일반화 금지.
- vector.c·`build_custom.sh`/`apply_patch.sh` 손대지 말 것.
- 새 측정 필요 시 서버 55435 만 (cache/rq3/latency/), nohup background.
- 비자명 작업은 ultraplanning → ExitPlanMode → 실행.
- **핵심 hallucination 회피**:
  - "CaseB latency < B1" generalize 금지 (paired Wilcoxon 168 중 13 만 유의 = 7.7%).
  - "B1 plan 회복" generalize 금지 (qid 의존 fragile, qid=0 = 0/4).
  - q3 sel0.01 probe (saturated, 2.6~3.5×) vs core 4 sel=0.001 (3-7×) 분리.

## 5. 시작
handoff 정독 → 보고서·deck·포스터 반영 계획 ultraplan → 승인 → §4 수치 정확 인용 + plan 매트릭스 시각화 + honest limitation 정직 보고 → 인계.
