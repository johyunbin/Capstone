@_internal/handoff/active/handoff_20260520_204200_PoC평면확장_prescan세션종료.md 정독 후 시작.

5/20 20:42 종료 — PoC 평면 확장 Phase A prescan + 자문 메일 _202200 최신화 + 4 엔진 future work framing 확정.

## 핵심 carry 3건

1. **DEEP sf=1 = PoC 무의미 확정 폐기** — 모든 16 variant injection_fired=False, plan 동일 (Gather Merge→Sort), median variance ≈1%. sf=1 작은 테이블 IndexScan 경로로 SeqScan 분기 안 타서 injection mechanism 우회.
2. **DEEP sf=100 시간 risk 정량** — estimates 20분 → 측정 추정 40-50 min/cell. 12 cell 약 16-20h, 24h budget 위험. **server background 진행 중** (bash pid 3587438 + python pid 3587449, sf=100 33분+ 진행 → SIFT sf=10 → SSN sf=10 sequential). **다음 세션 시작 즉시 결과 회수**.
3. **4 엔진 통합 PoC = future work 정직 framing 확정** — pgvector 단독, VBASE/DuckDB 빌드 15-25h vs 5/21 권한 종료. **Track B (Plan diff only PoC) 폐기**.

## 자문 메일 (사용자 발송 대기)

- 신규: `submission/_drafts/속도는벡터_3차 자문요청_20260520_202200.{md,pdf}` (2 page · 278 KB · 미발송)
- 박성원 멘토님 3차 자문. Phase 5 엔진 탑재 (94.9% plan 회복 + latency 동등 분리) + Phase 6 PoC (model3 condition 0.00% p=0.866) + Q9 sel=0.1 honest exception + 4 엔진 future work framing 흡수.
- 자문 요청 3 재구성: (1) 메시지 framing, (2) honest exception 위치, (3) 4 엔진 단일 한계 framing.
- 5/24 회신 일정 base → 5/27·29 발표 반영.

## 다음 세션 task (순서)

1. **[★ 시급] server prescan 결과 회수** — `ssh capstone 'ls /mnt/hdd0/home/capstone2026/cache/rq3/latency/phase4_prescan/*.json'`. 각 raw JSON jq 로 `injection_fired=True` 확인 + sf=100 실제 측정 시간 정량.
2. **Track A 평면 확정** — sf=100 risk base 사용자 결정 (전체 12 cell / sel=0.001 만 4 cell / q3 만 3 sel / 폐기). SIFT sf=10 + SSN sf=10 = 24 cell 확정.
3. **Phase B 측정 launch** — phase4_extension/{SIFT_sf10,SSN_sf10,DEEP_sf100_부분}/ server background + Monitor.
4. **Phase C analyze + stats_poc_6_4_extended.py 작성** — PoC 1·2·3 재실행 + 평면 일반화 비교 표.
5. **Phase E 보고서 §5.5 paragraph + 표 + §6.4 (3) 4 엔진 framing 정리** + PDF 재빌드 (compact ≤ 22p).
6. **Phase F Close** — handoff·archive·CLAUDE.md anchor·commit·push·맥북 sync.
7. **5/22 박광현 미팅 사전보고** — 보고서 _171521 + PoC summary + prescan 진행 (sf=1 폐기·sf=100 risk·dataset 확장) + 4 엔진 future work 안내.
8. **사용자 직접 — 자문 메일 _202200 발송** (박성원 멘토님 5/24 회신 일정).

## 작업 방식·정책

- 비자명 작업 = ultraplanning → ExitPlanMode 승인 → 실행
- 메인 = 서버 측정, 병렬 = 코드·분석·문서. 서버 동시점유 회피.
- 모델 = Opus 최신, 병렬 에이전트도 Opus
- `build_custom.sh`·`apply_patch.sh` 절대 금지 carry
- 발표물 코드명 노출 금지 / 보고서 코드명 OK
- 자료 추가 = 측정 데이터 확정 후 (skeleton 미리 짜고 수치 채우기 금지)

## 시작 명령

`@_internal/handoff/active/handoff_20260520_204200_PoC평면확장_prescan세션종료.md` 정독 후 → `ssh capstone 'ls -la /mnt/hdd0/home/capstone2026/cache/rq3/latency/phase4_prescan/*.json'` 으로 server 측 측정 진행 상황 확인부터.
