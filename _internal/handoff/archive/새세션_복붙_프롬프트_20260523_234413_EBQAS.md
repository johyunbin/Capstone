@_internal/handoff/active/handoff_20260523_234413_EBQAS_measure코드작성완료_smoke대기.md 를 self-contained 인계로 정독하고 EB-QAS 별도 트랙을 이어간다.

## Framing (불변, 압축)
본 트랙 = 강재현 팀원 카톡 발화(2026-05-23 21:48 KST) → 사용자 paste → 본 Claude 21:54 KST 정본화 → 22:20~22:51 인프라·v2 대조·spec 3건·Codex 적대 검증 결과 회수 → 23:00~23:30 Codex 5건 정정 spec patch 완료 → 23:30~23:54 **활성화 + measure_ebqas 코드 11 항목 + 4 unit test 8/8 PASS + Codex review 디스패치 spec 작성**까지 4 세션 진행. 분포 사전 지식 가정 X · history-aware empirical-Bayes로 Exqutor B1 자체 대체 (CaseB식 산술평균 X). 5/23 감사 평결과 4축 호환되는 별도 후속 연구 트랙(Codex 축 e pass 0.87). 사용자 결정(23:33 KST): 즉시 활성화 + 코드 작성 / CaseB comparator = strong-13 aggregate / 팀 공유 = 측정 결과 회수 후. 메인 트랙(v14 commit ffa55f09·close commit 54098a10 완료 — 다음 박성원 5/24 회신 반영·claude.ai/design pptx export 단계)과 분리.

## 이번 세션 작업
1. ★★★ Codex review 디스패치 → 결과 회수 → 6 축 verdict 분류. `_internal/state/ebqas_track/codex_검증/codex_measure_ebqas_review_spec_20260523_234413.md` §3.1 `codex exec --sandbox read-only` 명령 실행 (Mac-mini.local, xhigh). pass면 task 2 진입, concern·fail이면 코드/spec 정정 후 재디스패치.
2. ★★★ smoke 1 cell launch — Codex review pass 후. `measure_ebqas(cell, n_queries=1000, trials=10, prior_mode_init="history")` DEEP × sf=10 × sel=0.01. 자원 watchdog v4 적용. ~9분.
3. ★★ 24 cell sequential 측정 — smoke pass 후. DEEP·SIFT·WIKI × sf{1,10} × sel{0.001,0.01,0.10} × 2 prior_mode_init (24 cell). sequential — 메인 트랙 v14 패턴 carry. ~3.6 시간(절반 자원 가정 7~8 시간).
4. ★ paired 분석 4축 + 다음 carry handoff — 4 mode(B1·CaseB-strong13·EB-QAS·EB-QAS-no-history) paired join (assert_paired_join_invariant 통과 필수) → better_ratio + Wilcoxon + matched rank-biserial + bootstrap CI 일관 시 결론. subset 분할(plan_changed_vs_B1=True·low-selectivity·high-dimensional). 다음 carry handoff + 메모리 갱신 + 사용자 결정 시 팀 공유 메시지.

## 작업 방식·정책
- 한국어 학술 산문 + 영문 인용 verbatim.
- 비자명 작업은 ultraplanning → ExitPlanMode 승인 후 실행.
- 메인 트랙 손대지 않음 (handoff_20260523_230914 별도 carry).
- push는 사용자 명시 지시 시.
- 환각 회피: v13 수치 carry, "분포 인지 효과" 부활 금지, "EB-QAS가 B1보다 낫다" 단언 금지(측정 전·Codex review pass 후도). measure_ebqas 측정은 default `EBQASParams()` 사용(`w_mismatch=1` default, unit test의 `w_mismatch=0` override는 단순화 용도 only).
- 타임코드 네이밍: 모든 산출물 새 파일 생성. 본 세션 신규 = 측정 시점 KST `_YYYYMMDD_HHMMSS`.
- TaskUpdate로 task 추적.

## 시작 명령
python3 -c "from datetime import datetime, timezone, timedelta; now=datetime.now(timezone(timedelta(hours=9))); print(now.strftime('%Y-%m-%d %H:%M:%S KST (%A)'))"
cd ~/Capstone && git fetch origin && git status --short
