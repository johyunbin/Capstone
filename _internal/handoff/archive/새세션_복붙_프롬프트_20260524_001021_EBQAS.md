# 새세션 복붙 프롬프트 — EB-QAS 트랙 #6 세션 진입 (Codex review 결과 concern → 4.B 정정 분기)

> 본 프롬프트 = EB-QAS 별도 트랙 #6 세션의 진입 anchor. 새 세션 첫 입력으로 그대로 복붙. 본 #5 세션이 Codex measure_ebqas review를 디스패치·결과 회수·6 축 verdict 분류·**concern 4건 → 4.B 분기 결정**까지 완료한 시점부터 carry.

---

@_internal/handoff/active/handoff_20260524_001021_EBQAS_codexreview결과_concern_정정필요.md 를 self-contained 인계로 정독하고 EB-QAS 별도 트랙을 이어간다.

## Framing (불변, 압축)

본 트랙 = 강재현 팀원 카톡 발화(2026-05-23 21:48 KST) → 사용자 paste → 본 Claude 21:54 KST 정본화 → 22:20~22:51 인프라·v2 대조·spec 3건·Codex 적대 검증 결과 회수 → 23:00~23:30 Codex 5건 정정 spec patch 완료 → 23:30~23:54 활성화 + measure_ebqas 코드 11 항목 + 4 unit test + 4 헬퍼 test (8/8 PASS) + Codex review 디스패치 spec → **00:03~00:20 Codex review 디스패치(prompt file + stdin /dev/null, Mac-mini.local, session 019e5565-fab1, xhigh + gpt-5.5, 8분) → 결과 회수 → 6 축 verdict 분류 → 종합 verdict concern (fail 0건, concern 4건, pass-with-concern 1건, pass 1건)**까지 5 세션 누적 진행. 분포 사전 지식 가정 X · history-aware empirical-Bayes로 Exqutor B1 자체 대체 (CaseB식 산술평균 X). 5/23 감사 평결과 4축 호환되는 별도 후속 연구 트랙(Codex 축 e pass-with-concern 0.87, #5 세션). 사용자 결정(23:33 KST): 즉시 활성화 + 코드 작성 / CaseB comparator = strong-13 aggregate / 팀 공유 = 측정 결과 회수 후. 메인 트랙(handoff_20260523_230914 박성원 5/24 회신·5/26 LearnUs critical path) 분리.

## 핵심 수치·결과 (v13 정본 carry + 본 #5 세션 Codex review)

- v13 3-way matched 1508: CaseB vs B1 89.1%/−4.38%(인과 귀속 폐기) · CaseA vs B1 35.2%/+12.90%(악화) · 고정-N B1 1.944 / CaseA 1.984 / CaseB 1.477 / CaseB′ 1.459 · hyperloglog −4.58% · latency 56 cell +0.13%·r=−0.007 · v14 CaseC 9 cell 평균 1.373
- 직전 Codex #2(225122): 6 축 종합 concern — (a) 0.86 / (b) **fail 0.91** / (c) 0.82 / (d) 0.84 / (e) **pass 0.87** / (f) 0.88 → #3 spec 5건 정정 patch
- #4 measure_ebqas 코드 11 항목 (라인 1582~2144) + test 8/8 PASS
- **★ 본 #5 Codex review 결과 (001021)**: 종합 verdict **concern**:
  - (a) 수학 = **concern 0.86** — `params.n_cap` dead param (ebqas_estimate가 state.n_cap만 사용), `q_log_floor`·`gamma` 미반영
  - (b) 안전장치 = **fail→concern 0.82** — explicit mode switch 구현 OK이나 `no_history`가 실제로 history 끄지 못함 (alpha/beta 계속 갱신)
  - (c) paired = **concern 0.78** — invariant key (trial_idx, query_idx)만, cell·seed 미검증, B1·CaseB schema 없으면 2-mode fallback
  - (d) leakage = **concern 0.84** — group key 자체는 OK, `template_id=q_row_idx` cold-start risk
  - (e) 평결 호환성 = **pass-with-concern 0.87** — EB-QAS-no-history label (b)와 같은 사유로 의미 불일치
  - (f) 외부 인용 = **pass→pass 0.90** — #3 v4 외부 fetch + clean source map 효과
- **★ Codex 결론 verbatim**: "smoke 1 cell은 직접 함수 호출 기준으로 진행 가능하지만, **24 cell sequential 측정 진입 전에는 `params.n_cap`, `no_history` 의미, paired invariant 3건은 고치는 편이 맞습니다.**"

## 이번 세션 작업 — **4.B concern 분기 진입** (선정 carry)

본 #5 review 결과 = fail 0건 + concern 4건이라 plan 분기 표 기준 4.A pass 분기는 minor concern≤1 조건 미충족 부적합, 4.C fail 분기도 부적합. **4.B concern 분기 진입이 정합**. 단 Codex 결론 hybrid (smoke OK + 24 cell 전 정정 필수)이므로 4.B 안에서도 smoke 단독 launch가 함수 호출 기준 가능.

1. **★★★ 정정 plan 작성** — `_internal/state/ebqas_track/codex_검증/codex_review_결과_001021.md` §3-§8 6 축 finding 정독 → 정정 plan `~/.claude/plans/<slug>.md` 작성 → ExitPlanMode 승인. 6 항목(우선순위 1~3) × (대상 파일·라인·정정 방향·회귀 test·검증).
2. **★★★ 코드 정정 우선순위 1 (3 항목)**:
   - (a) `_internal/scripts/measure_paper_exact.py:1700` `ebqas_estimate`에서 `state.n_cap` → `params.n_cap` (또는 state init 시 주입). 회귀 test `test_n_cap_param_honored` 추가
   - (b) `ebqas_estimate`(라인 1725) 또는 `update_after_execution`(라인 1812)에서 `state.prior_mode=="no_history"` 분기 시 alpha=beta=1.0 override 또는 update skip. 회귀 test `test_no_history_keeps_alpha_beta_neutral` 추가
   - (c) `assert_paired_join_invariant`(라인 2090) key 확장 `(cell, trial_idx, seed, query_idx)` + 모든 mode 모든 row true_cardinality 비교. 또는 B1/CaseB schema 확장(query_results 도입)
3. **★★ 코드 정정 우선순위 2 (1 항목)** — (d) smoke 시 `n_groups / n_queries` launch gate 기록. `q_row_idx`(라인 1940) template vs query identity 명시
4. **★★ 코드 정정 우선순위 3 (2 항목)** — (e) (b) 정정 시 자동 해결 또는 label 변경 `EB-QAS-no-stop` / (f) `q_log_floor`·`gamma` dead param 정리
5. **★ 정정 후 본 review 재디스패치** — 같은 spec(`codex_measure_ebqas_review_spec_20260523_234413.md`) 또는 정정 spec 사용 → 새 log path → 결과 회수·분류 → pass면 4.A 진입 (smoke → 24 cell → paired 분석)
6. **★ 또는 smoke 단독 launch (병행 OK)** — Codex 결론 "smoke 1 cell은 함수 호출 진행 가능"이므로 EB-QAS mode 단독 smoke (DEEP × sf=10 × sel=0.01)는 정정 완료 전 launch 가능. 단 EB-QAS-no-history mode 비교는 정정 후로 미룸

## 작업 방식·정책

- 한국어 학술 산문 + 영문 인용 verbatim.
- 비자명 작업은 ultraplanning → ExitPlanMode 승인 후 실행.
- 메인 트랙 손대지 않음 (handoff_20260523_230914·handoff_20260524_000200 별도 carry).
- push는 사용자 명시 지시 시.
- 환각 회피: v13 수치 carry, "분포 인지 효과" 부활 금지, "EB-QAS가 B1보다 낫다" 단언 금지(측정 전·Codex review pass 후도). Codex review 결과 인용 시 verbatim — 우리 해석으로 verdict surrogate 금지. measure_ebqas 측정은 default `EBQASParams()` 사용(`w_mismatch=1` default, unit test `w_mismatch=0` override는 단순화 용도 only).
- 타임코드 네이밍: 모든 산출물 새 파일 생성. 본 세션 신규 = 측정 시점 KST `_YYYYMMDD_HHMMSS`.
- TaskUpdate로 task 추적.

## 시작 명령

```bash
python3 -c "from datetime import datetime, timezone, timedelta; now=datetime.now(timezone(timedelta(hours=9))); print(now.strftime('%Y-%m-%d %H:%M:%S KST (%A)'))"
cd ~/Capstone && git fetch origin && git status --short
```

## 작업 방식·정책

- 한국어 학술 산문 + 영문 인용 verbatim.
- 비자명 작업은 ultraplanning → ExitPlanMode 승인 후 실행.
- 메인 트랙 손대지 않음 (handoff_20260523_230914 별도 carry).
- push는 사용자 명시 지시 시.
- 환각 회피: v13 수치 carry, "분포 인지 효과" 부활 금지, "EB-QAS가 B1보다 낫다" 단언 금지(측정 전·Codex review pass 후도). Codex review 결과 인용 시 verbatim — 우리 해석으로 verdict surrogate 금지. measure_ebqas 측정은 default `EBQASParams()` 사용(`w_mismatch=1` default, unit test `w_mismatch=0` override는 단순화 용도 only).
- 타임코드 네이밍: 모든 산출물 새 파일 생성. 본 세션 신규 = 측정 시점 KST `_YYYYMMDD_HHMMSS`.
- TaskUpdate로 task 추적.

## 시작 명령

```bash
python3 -c "from datetime import datetime, timezone, timedelta; now=datetime.now(timezone(timedelta(hours=9))); print(now.strftime('%Y-%m-%d %H:%M:%S KST (%A)'))"
cd ~/Capstone && git fetch origin && git status --short
```
