# server_backup_20260520 — 서버 권한 종료(5/21) 전 backup
# 50MB 초과 file 또는 외부 재현 가능한 file 은 git에서 제외
# (로컬 backup 본은 디스크에 유지)

# Exqutor 외부 GitHub repo 의 .git 폴더 (재clone 가능)
Exqutor/.git/

# 큰 측정 raw csv (RQ1 phase7) — 외부 데이터 carry, 76MB
cache/rq1/phase7_8m_strata.csv

# 일반 패턴
*.pack
__pycache__/
*.pyc

# nested .git (submodule link / .git 디렉토리) — 외부 repo 재clone 가능
**/.git/
**/.git
