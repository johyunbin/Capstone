# 강재현 5/13 0:20 피드백 답변 가이드 (줄글 형식)

> **수신**: 강재현 + 박세은 (속도는벡터 카톡 그룹)  
> **발신**: 조현빈  
> **시점**: 5/13 0:25 KST  
> **형식**: 줄글 산문 (박세은 의도 유지)

---

## 답변 카톡 paste 형태

```
재현아 답변할게.

1번. multi-join 후 stratification 학습 추가 테스트는 좋은 제안이야. 본 측정에서는 single table 의 K-means 20 cluster 를 join 결과에 그대로 carry-over 하는 방식이라 join 결과 자체의 분포는 학습이 안 된 상태야. 두 테이블을 먼저 결합한 후 join 결과의 cluster 분포를 새로 학습하면 multi-table cell 의 정확도가 더 개선될 가능성이 있어. 마찬가지로 km10, km30 같은 cluster 수 변화 실험도 cluster granularity 의 영향을 직접 측정할 수 있는 좋은 방향이야.

다만 본 시점에서는 서버 사용 정책상 (박세은이 채림님께 서버 사용 안 한다고 보고 + 5/12 동의) 추가 측정은 폐기 결정된 상태야. 본 한계는 5/27 발표 자료의 Limitation slide 에 "multi-table 영역은 single table 의 stratification carry-over 방식 사용, 별도 join 결과 stratification 학습 미진행" 으로 명시할 계획이야. km10/km30 cluster 수 변화도 같은 limitation 영역에 추가 가능. 향후 측정 영역 (cluster granularity sensitivity, multi-table re-stratification) 으로 5/27 발표 또는 6/11 최종보고서에 future work 로 표명할 수 있어.

2번과 3번 함께. 정의 mapping 은 정확해 — paradigm = 추정 방법의 분류 카테고리 (밀도 추정 / 정보 이론 / 스트리밍 등 8 개), method = 각 paradigm 안의 구체 알고리즘 (총 56 개). paradigm 안에 method 가 여러 개 들어 있는 1:N 관계야. 예를 들어 차원 축소 paradigm 안에는 Sparse Random Projection 외에도 pca1d, cca1d, ica_fastica, tucker, rsvd, neuram 등 12 개 정도의 method 가 있고, 공간 분할 paradigm 안에는 Hilbert curve 외에 zorder_morton, skilling_hilbert, idistance, lpm1_proper 등 12 개 정도가 있어.

이전 답변에서 paradigm 별로 논문 1편씩 알려준 것은 각 paradigm 의 "대표 (anchor) method" 의 논문만 mention 한 거야. 본 측정 결과 가장 큰 정확도 개선을 보인 5 paradigm 의 대표 알고리즘이 다음과 같아 — 밀도 추정의 Parzen KDE / 정보 이론의 HyperLogLog / 스트리밍의 Chao 1982 weighted reservoir / 차원 축소의 Sparse RP / 공간 분할의 Hilbert curve. 이 5 개가 발표 자료 슬라이드 17 의 "가장 우수 알고리즘 5선" 이고, paradigm 안의 나머지 method 들은 paradigm rollup 평균값에 함께 포함되지만 슬라이드에서는 개별 mention 하지 않아.

mapping 을 정리하면 paradigm 8 개가 큰 분류이고, 각 paradigm 안에 method 가 평균 7 개씩 있어서 총 56 method 가 돼. 각 paradigm 의 대표 method 1 개 — 가장 잘 알려진 또는 본 측정에서 가장 큰 개선을 보인 알고리즘 — 만 발표 자료에 강조하는 구조야.

4번. ensemble 비용 limitation 명시 OK 해줘서 고마워. 발표 자료 Limitation slide 에 그대로 들어갈 거야.
```

---

## v4 deck 정정 plan (강재현 1번 피드백 반영)

Limitation slide 에 추가:

```
column 추가: STRATIFICATION SCOPE
  Eyebrow: SCOPE
  큰 텍스트 (24px): "multi-table re-stratification 미진행"
  caption (14px fg3):
    "본 측정 multi-table cell (A2-Fig7/Fig9) 에서
     single table KM20 cluster 를 join 결과에 carry-over.
     두 테이블 join 후 별도 stratification 학습 X.
     향후 측정 영역 (cluster granularity sensitivity:
     km10/km20/km30 비교, multi-table re-stratification)"
```

5/27 deck v5 정정 시 (5/16 토 한도 reset 후 또는 manual edit PPTX) 반영.

---

작성: 2026-05-13 00:25 KST
