# v18 → v19 polishing prompt (slide 2·3·5·6·7 fix)

> 작성 2026-05-26 00:18 KST · 사용자 피드백 (5/25 23:54 + 26 00:14) 누적 정밀 반영
> 적용처: claude.ai/design 동일 대화창 `019e1a41-701c-7134-9ce1-1247262c1563`
> 기존: v18 14 slide deck (`____ (2).pptx`, 5/25 13:30 다운로드)
> 결과: v19 14 slide deck — 단 **slide 2·3·5·6·7 만 정밀 polishing**, 나머지 **9 carry slide (1·4·8·9·10·11·12·13·14) 변경 없음**
> 본 파일이 최신. 직전 3slide prompt (`v18_polishing_prompt_3slide_5_26_00_05.md`) 는 carry (히스토리).

---

## ▼ ▼ ▼ 단일 복붙 시작 ▼ ▼ ▼

기존 v18 14 slide deck 에서 **slide 2·3·5·6·7 만 정밀 polishing** 진행합니다. 다른 9 slide (1·4·8·9·10·11·12·13·14) 는 **변경 없음 carry**.

design system (navy `#1E3A5F` · cyan `#0EA5E9` · purple `#8B5CF6` · green `#10B981` · coral `#F97316` · Apple SD Gothic Neo · 흰 배경 · chapter badge `배경/방법/결과/적용` · hero grad slide 11·13 등) 동결. 절대 규칙 9 (코드명 노출 금지 · 영역 필러 금지 · 수식 노출 금지 · 이분법 강조 금지 · 별표 ★ 노출 금지 · 페이지 번호 부재 · 텍스트 잘림·겹침 금지 · design system 동결 · 한국어 라벨 통일) 모두 carry. 단 slide 7 베이스라인 라벨은 사용자 명시 요청으로 "baseline" 영문 변경 + 풀어쓰기 caption 추가 (예외).

---

## 전 슬라이드 공통 룰 (slide 2·3·5·6·7 모두 적용)

### 1. 약어 처음 등장 시 풀어쓰기 한 번 노출

모든 약어는 처음 등장 시 한국어 풀이를 함께 노출. 예시 형식:

- VAQ → **"벡터 증강 분석 쿼리 (Vector-Augmented Query, VAQ)"**
- HNSW → **"계층적 탐색 가능한 작은 세계 (Hierarchical Navigable Small World, HNSW)"**
- ECQO → **"Exqutor 의 Cardinality-aware Query Optimization (ECQO)"**
- TPC-H → **"표준 분석 벤치마크 (TPC-H)"** — 단 slide 2·3 에서는 TPC-H 단어 자체를 제거 (사용자 요청)
- Adaptive Sampling → **"Adaptive Sampling (적응적 표본 추출)"**
- DEEP → **"벡터 데이터셋 DEEP"**
- baseline → **"baseline (= 논문 원본 · 무작위 베르누이)"**

### 2. 텍스트 줄 정렬 일관성

- 모든 텍스트 블록은 **좌측 정렬** (또는 명백한 가운데 정렬) 통일
- 들여쓰기·padding 동일 그리드 (16-20px)
- 라벨·캡션 폰트 크기·color 일관
- 세로 회전 텍스트 (90° / 180°) 금지 — 모든 라벨 가로 정상 표기

### 3. "너무 클로드 출력같은" 표현 회피

- "paper carry · Exqutor §V-B", "결과 표시 X · 본 발표 미포함", "본 연구 핵심" 등 메타 라벨 모두 삭제
- "한 측정 cell" 같은 영문 코드 단어 → 자연 한국어 ("같은 쿼리 · 같은 데이터에서") 변경
- 자연스러운 한국어 발화체 우선, 영문 약어·코드명 노출 최소화

---

## Slide 2 — 배경 (벡터 증강 분석 쿼리 · VAQ) 정밀 변경

### 현 v18 시각 (간략)

- 제목 "벡터 증강 분석 쿼리 — VAQ" (VAQ 아래 점선 underline)
- 분석가 박스 (왼쪽): "이 부품 사진이랑 비슷한 부품들이 / 최근 가장 많이 팔린 주문은?" + 사람 icon + "손가락 → SQL"
- SQL 박스 (가운데): caption "한 SQL 안에 — VAQ / TPC-H Q3 변형 — 한 SQL 안에 관계형 JOIN + 벡터 유사도" + SQL 코드 + "← 관계형 조건 (TPC-H)" 라벨 + "← 벡터 유사도 (VAQ)" 라벨
- VAQ 결과 (오른쪽): "유사 벡터 추출" + customer·orders·lineitem·partsupp_deep bullet + "매출 TOP 행 / 유사 부품 매출 분석 결과" 박스

### 변경 사항 6 건

1. **제목 풀어쓰기** — "벡터 증강 분석 쿼리 — VAQ" → **"벡터 증강 분석 쿼리 (Vector-Augmented Query, VAQ)"** (한 줄, VAQ 점선 underline 제거)

2. **SQL 박스 caption 단순화** — "한 SQL 안에 — VAQ / TPC-H Q3 변형 — 한 SQL 안에 관계형 JOIN + 벡터 유사도" → **"예시 분석 쿼리 — 관계형 조건과 벡터 유사도를 한 SQL 안에"** (단일 줄, TPC-H 단어 제거)

3. **SQL 옆 라벨 풀어쓰기**:
   - "← 관계형 조건 (TPC-H)" → **"← 관계형 조건 (예: 가정용 카테고리 · 1995-03-14 이전 주문)"**
   - "← 벡터 유사도 (VAQ)" → **"← 벡터 유사도 — 임베딩 비교"**

4. **분석가 박스 carry** — 박세은 피드백 자연어 그대로 유지: "이 부품 사진이랑 비슷한 부품들이 / 최근 가장 많이 팔린 주문은?"

5. **오른쪽 결과 박스 직관화**:
   - "유사 벡터 추출" → **"유사 부품 추출"** (한국어)
   - 4 컬럼 bullet (customer · orders · lineitem · partsupp_deep) — carry, 단 옆에 "← 관계형 데이터" / "← 벡터 데이터" 한 번 표시
   - "매출 TOP 행 / 유사 부품 매출 분석 결과" → **"→ 유사 부품의 매출 상위 주문 도출"** (단일 명확)

6. **텍스트 줄 정렬 통일** — 분석가 박스·SQL 박스·결과 박스 모두 좌측 정렬, padding 16px, 라벨 폰트 크기 11.5pt 통일

---

## Slide 3 — 배경 (카디널리티 한 곳이 잘못되면 — 최대 1만 배) 정밀 변경

### 현 v18 시각 (간략)

- 제목 "카디널리티 한 곳이 잘못되면 — 최대 1만 배 느려짐"
- 부제 "벡터 테이블 100만 행 · 같은 SQL · 같은 데이터" — 가로 라인 디자인 요소 부근 (시각 겹침)
- 왼쪽 박스 (빨간 테두리, 잘못된 plan) + 오른쪽 박스 (cyan 테두리, 정확한 plan) — plan tree 그래픽 (⋈ + σ + 점선 + cyan 점선 타원)
- 하단 hero "10,000× 응답 시간 차이" + 메타 "TPC-H Q3 VAQ on DEEP"

### 변경 사항 5 건

1. **부제 위치 fix** — 제목과 부제 사이 padding-top 20px 확보, 부제는 별도 line 처리. 가로 라인 디자인 요소 (cyan stripe) 와 시각 겹침 해소.
   - 부제 텍스트 carry: "벡터 테이블 100만 행 · 같은 SQL · 같은 데이터"

2. **plan tree node 옆 한국어 풀이 라벨 추가** (직관성 보강):
   - 왼쪽 plan (잘못): "Hash · 한꺼번에 묶기" / "Seq Scan · 전체 행 읽기" caption 추가
   - 오른쪽 plan (정확): "Nested Loop · 하나씩 인덱스로" / "Index Scan · 인덱스로 부분만" caption 추가
   - 왼쪽 "333,333 행 통째 메모리" → **"전체 벡터 100만 행 메모리 폭주"**
   - 오른쪽 "~100점만 조회" → **"인덱스로 100점만 직접 조회"**

3. **양쪽 박스 하단 캡션 직관화**:
   - 왼쪽: "큰 중간 테이블 누적 → 메모리·시간 폭주" → **"전체 행을 모두 메모리에 → 100만 행 폭주"**
   - 오른쪽: "한 행씩 인덱스로 정확히 풀어냄" → **"인덱스로 100점만 직접 조회 → 즉시"**

4. **hero "10,000× 응답 시간 차이" carry** — 그라데이션·크기 변경 없음

5. **메타 풀어쓰기**: "TPC-H Q3 VAQ on DEEP" → **"벡터 데이터셋 DEEP · 표준 분석 쿼리 예시"**

---

## Slide 5 — 방법 (인덱스 없을 때 Adaptive Sampling) 정밀 변경

### 현 v18 시각 (간략)

- 제목 "인덱스가 없을 때 — Adaptive Sampling"
- 4 STEP 가로 흐름: Query → ① 표본 추출 ★ → ② 카디널리티 추정 → ③ Q-error 측정 → ④ Momentum 조정
- 하단 BATCH 흐름: BATCH 1 · Q₁-Q₅₀ → BATCH 2 · Q₅₁-Q₁₀₀ → BATCH 3 · Q₁₀₁-Q₁₅₀ + "N 갱신" 세로 회전 텍스트 + m=0.9 · η 감쇠

### 변경 사항 7 건

1. **제목 풀어쓰기** — "인덱스가 없을 때 — Adaptive Sampling" → **"인덱스가 없을 때 — Adaptive Sampling (적응적 표본 추출)"**

2. **STEP ④ 박스 단어 변경**: "Momentum 조정 (다음 표본 크기)" → **"표본 크기 조정 (다음 표본은 더 많이 / 적게)"** (Momentum 단어 제거)

3. **STEP ① 박스 caption 단순화**:
   - 현: "무작위 베르누이 · 균일 random / 한 쿼리당 N=385 sample 1회 추출 → 50 쿼리마다 N 갱신"
   - 신: **"무작위 균일 추출 (베르누이 방식) · 한 쿼리당 표본 385개 한 번"**

4. **BATCH 라벨 풀어쓰기** (Q 약어 제거):
   - "BATCH 1 · Q₁-Q₅₀" → **"샘플링 1~50회 (1단계)"**
   - "BATCH 2 · Q₅₁-Q₁₀₀" → **"샘플링 51~100회 (2단계)"**
   - "BATCH 3 · Q₁₀₁-Q₁₅₀" → **"샘플링 101~150회 (3단계)"**
   - 상단 "UPDATE PERIOD · 50 쿼리마다 N 자체가 갱신" → **"50회 샘플링마다 표본 크기 자체가 갱신"**

5. **★ "N 갱신" 세로 회전 텍스트 제거** (시각 버그 fix):
   - 회전 제거, 가로 정상 표기로 변경
   - 화살표 위 caption: **"→ 표본 크기 N 갱신"** (가로, font 11pt navy)

6. **m=0.9 / η 감쇠 파라미터 모두 삭제**:
   - BATCH 1 박스 안 "초기 N · 각 쿼리 sample 1회 → Q-error" → **"초기 표본 크기 385개 · 각 쿼리 한 번 추출 → 오차 측정"**
   - BATCH 2 박스 안 "누적 Q-error · m=0.9 · η 감쇠" → **"누적 오차로 N 자체 갱신"**
   - BATCH 3 박스 안 "누적 Q-error → N 또 갱신 · 반복" → **"누적 오차로 N 또 갱신 · 반복"**
   - 하단 캡션 → **"한 쿼리 = 표본 1회 추출 · 50 쿼리 모이면 표본 크기 N 자체가 갱신"**

7. **본 연구 집중 단계 ★ badge carry** — STEP ① 박스의 "본 연구 집중 단계 ★" purple badge 유지 (★ 별표 노출 단 이 위치만 절대 규칙 6 예외).

---

## Slide 6 — 방법 (본 연구 · Research Question) 정밀 변경

### 현 v18 시각 (간략)

- chapter badge: 방법 (purple)
- 제목: "본 연구" (큰 hero, purple underline)
- 왼쪽 큰 박스 (purple border, 약 60% width): "RESEARCH QUESTION" eyebrow + "카디널리티 추정을 더 잘할 수 있는 / 표본 추출 방식은 무엇일까?" (큰 navy 텍스트)
- 오른쪽 (40% width) 세로 2박스:
  - 위: "무작위 베르누이" + cyan 점 cluster (균일 무작위 점들)
  - 가운데 caption: "→ 비교"
  - 아래: "샘플링 방식 탐색" (purple border) + 다양한 패턴 시각 + "다양한 패턴 묶음 — 클러스터·곡선·해시 등"
- 하단 텍스트: "샘플링 방식 탐색 → 무작위 베르누이와 대조"

### 변경 사항 4 건

1. **layout 2x2 grid 재배치** (가로 풀폭):
   - **1행 (col 1~2 가로 전체)**: RESEARCH QUESTION 박스 — 현 왼쪽 박스 그대로 carry 하되 **가로 전체 폭으로 확장**. eyebrow "RESEARCH QUESTION" + 큰 navy 텍스트 "카디널리티 추정을 더 잘할 수 있는 표본 추출 방식은 무엇일까?" 한 줄 또는 두 줄 (전체 가로 폭 fit).
   - **2행 col 1 (왼쪽)**: **"무작위 베르누이" 박스** — 현 오른쪽 위 박스 carry (cyan 점 cluster + "균일 무작위 점들" caption). 박스 폭 = 50% (가로 1/2).
   - **2행 col 2 (오른쪽)**: **"샘플링 방식 탐색" 박스** — 현 오른쪽 아래 박스 carry (purple border, 다양 패턴 + "다양한 패턴 묶음 — 클러스터·곡선·해시 등" caption). 박스 폭 = 50%.

2. **두 박스 사이 대조 신호 강조** — 현 작은 "→ 비교" caption 제거, **두 박스 사이 (2행 col 1 ↔ col 2 가운데)** 에 **명확한 대조 표시**:
   - 옵션 (A · 추천): 가로 양방향 화살표 **`↔`** purple 큰 (24-32pt, font weight bold) + 아래 caption "대조"
   - 또는 옵션 (B): 두 박스 사이 가운데 **purple chip "대조"** (큰 라벨, padding 8px, font 14pt bold)
   - 시각 위치: 2행 가운데, 두 박스 모서리에 화살표 양 끝 닿음

3. **하단 텍스트 "샘플링 방식 탐색 → 무작위 베르누이와 대조" 완전 삭제**

4. **2행 두 박스 시각 위계 정합**:
   - 양쪽 박스 height 동일 (256-280px)
   - 박스 안 그림 영역 / 라벨 / caption padding 동일
   - 라벨 폰트 크기 동일 (헤더 18pt · caption 12pt)

---

## Slide 7 — 방법 (통제 실험 · 세 방식 동시 산출) 정밀 변경

### 현 v18 시각 (간략)

- chapter badge: 방법 (purple)
- 제목: "통제 실험 설계 — 한 측정 안에 세 방식 동시 산출" (purple underline)
- 상단 가는 호 + 호 위 caption: "한 측정 cell 에서"
- 3 카드 가로 grid:
  - **베이스라인** (navy 박스, 왼쪽): 점 cluster + "↓ 추정값 1개" + "↓ 평균" + 하단 "paper carry · Exqutor §V-B"
  - **단독 대체** (회색 박스, 가운데): 큰 X 표시 + "↓ 추정값 1개 (회색)" + 하단 "결과 표시 X · 본 발표 미포함"
  - **결합** (cyan 박스, 오른쪽, **CORE** badge): 점 cluster + "↓ 추정값 A" "↓ 추정값 B" + "↓ 산술 평균" + "평균" 큰 박스 + 하단 "본 연구 핵심"
- 하단 캡션: "동시 산출 → 외생 변수 통제"

### 변경 사항 8 건

1. **제목 변경 (자연 한국어)**: "통제 실험 설계 — 한 측정 안에 세 방식 동시 산출" → **"동일 쿼리에 세 가지 추정 방식을 동시 적용"** (또는 "같은 쿼리·같은 데이터에서 세 추정 방식 동시 측정")

2. **상단 호 caption 변경**: "한 측정 cell 에서" → **"같은 쿼리 · 같은 데이터에서"** (코드 단어 제거)

3. **★ 베이스라인 박스 라벨 + 풀어쓰기**:
   - 현 "베이스라인" → **"baseline"** (영문 라벨, navy)
   - 박스 상단 (라벨 바로 아래) **풀어쓰기 caption 추가**: **"= 논문 원본 · 무작위 베르누이 방식"** (font 12pt navy soft)
   - 박스 하단 "paper carry · Exqutor §V-B" **완전 삭제**

4. **단독 대체 박스 + 풀어쓰기**:
   - 현 "단독 대체" carry (한국어)
   - 박스 상단 (라벨 바로 아래) **풀어쓰기 caption 추가**: **"= 베르누이를 분포 인지 표본으로 완전 치환"** (font 12pt grey)
   - 박스 하단 "결과 표시 X · 본 발표 미포함" **완전 삭제** — 단 박스 회색 흐림 효과 (현재 회색 박스) + 큰 X 표시 carry 로 "발표 미포함" 시각만 노출

5. **결합 박스 + 풀어쓰기**:
   - 현 "결합" carry (cyan)
   - 박스 상단 (라벨 바로 아래) **풀어쓰기 caption 추가**: **"= baseline + 분포 인지 추정값의 산술 평균"** (font 12pt cyan)
   - 박스 하단 "본 연구 핵심" **완전 삭제** — 단 CORE badge (cyan chip, "CORE") carry

6. **세 박스 내부 흐름 직관화** (앞 슬라이드 비교 framing 연결):
   - **baseline**: 무작위 베르누이 점 cluster → "↓" → "추정값 1개" → "↓" → "값" (단일 추정)
   - **단독 대체**: 분포 인지 점 cluster (회색, 큰 X 표시) → "↓" → "추정값 1개 (회색)" — 발표 X
   - **결합**: 베르누이 점 cluster (navy) + 분포 인지 점 cluster (cyan) **양쪽 모두 표시** → "↓ 추정값 A (베르누이)" + "↓ 추정값 B (분포 인지)" → "↓ 산술 평균" → "결합 추정값" (큰 cyan 박스)
   - 각 박스 안 점 cluster 옆에 작은 라벨 ("무작위", "분포 인지") 추가 — 청중이 어떤 방식인지 즉시 인지

7. **하단 캡션 carry**: "동시 산출 → 외생 변수 통제" 그대로 유지 (자연 한국어, 직관 OK).

8. **텍스트 정렬·padding 통일**:
   - 세 박스 height 동일 (~480-520px)
   - 라벨 폰트 크기 동일 (24pt 헤더 · 12pt caption)
   - 박스 내부 padding 동일 (24px)
   - 박스 모서리 radius 동일 (8px)

---

## 변경 X — carry slide (9 slide)

slide **1·4·8·9·10·11·12·13·14** 모두 v18 그대로 carry. 시각·텍스트·자산 모두 PNG byte 동일 검증 가능.

---

## ▲ ▲ ▲ 단일 복붙 끝 ▲ ▲ ▲

## 적용 방법

1. claude.ai/design 동일 대화창 (`019e1a41-701c-7134-9ce1-1247262c1563`) 진입
2. 본 prompt 의 ▼ ~ ▲ 구간 전체 복붙
3. v19 14 slide PPTX 다운로드 → 파일명 예상 `____ (3).pptx` 또는 직접 명명
4. Claude Code 가 v19 14 slide 추출 + visual 정합 검증 (5 polishing slide 변경 + 9 carry slide PNG byte 동일)
5. 사용자가 slide 1·4·8·9·10·11·12·13·14 순차 검토 진행

## 다음 세션 task carry (slide 1·4·8·9·10·11·12·13·14)

v19 적용 후 다음 9 slide 도 사용자 검토 → 추가 polishing prompt 누적 작성 → v20 → 최종 LearnUs 업로드 (5/26 23:59 마감)

**전 슬라이드 공통 룰 (carry)**:
- 약어 처음 등장 시 풀어쓰기
- 텍스트 줄 정렬 일관성
- "너무 클로드 출력같은" 표현 회피 (메타 라벨 삭제, 자연 한국어 우선)
- 수치 강조보다 시각 자료 활용
- 청중 5-7 초 안에 읽을 수 있는 정도
- 코드명·기술 용어는 한국어 풀이 동반
