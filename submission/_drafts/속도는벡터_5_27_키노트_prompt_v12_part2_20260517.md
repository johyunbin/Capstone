# 5/27 키노트 deck v12 — Part 2/3 (slide 1-15 상세)

**★ Part 1/3 paste 후 본 Part 2/3 paste — slide 1-15 상세**

---

## Slide 1 — title + 팀 소개

### Layout
- 중앙 거대 폰트 "속도는벡터" (Apple SD Gothic Neo Bold, 200px+, navy #1e3a5f)
- 그 아래 sub: "Distribution-aware Sample Selection for VAQ" (Inter Regular, 60px, 회색)
- 하단 4 팀원: 박세은 (팀장) / 강재현 / 조현빈 / 이동욱 (Inter Light, 36px)
- 하단 좌측: 연세대학교
- 하단 우측: 2026.05.27 (FINAL)
- 배경: 흰색
- 시각 요소: 우상단 mini accent line (navy, 4px, 100px)

### speaker note
> "안녕하세요. 연세대학교 캡스톤 디자인 팀 속도는벡터입니다. 박세은, 강재현, 조현빈, 이동욱 네 명으로 구성된 팀이고, 발표는 박세은 팀장과 조현빈이 나눠 진행합니다. 본 발표 주제는 벡터 증강 분석 쿼리를 위한 분포 인지 표본 선택입니다. paper Exqutor를 출발점으로, 표본 선택 단계를 분포 인지 방식으로 교체했을 때 cardinality 추정이 어떻게 달라지는지를 측정했습니다."

---

## Slide 2 — main theme

### Layout
- 중앙 거대 폰트: "**Distribution-aware**" (200px, navy #1e3a5f)
- 다음 줄: "**Sample Selection**" (200px, navy)
- 그 아래 sub (작은 폰트, 회색): "for VAQ Cardinality Estimation"
- 하단: "based on Exqutor §V-B Adaptive Sampling" (Inter Light, 32px)
- 우측 작은 박스 (subtle): "paper Exqutor의 cardinality 추정은 그대로, sample selection 단계만 교체"
- 배경: 흰색
- 시각: 우하단 작은 화살표 (navy, paper → 교체를 표현)

### speaker note
> "본 발표의 핵심 주제는 분포 인지 표본 선택입니다. paper Exqutor §V-B Adaptive Sampling을 출발점으로 삼습니다. 한 가지 분명히 할 점은, cardinality 추정 전체는 paper의 contribution이라는 것입니다. 저희가 손을 댄 곳은 그 추정에 들어가는 입력, 즉 표본을 어떻게 뽑느냐 하는 sample selection 단계 하나입니다. 이 구분이 발표 내내 일관되게 유지됩니다."

---

## Slide 3 — framing layer 분리

### Layout
- 중앙 상단 제목: "**framing layer 분리**" (Inter Bold, 80px, navy)
- 좌측 column (50%, 회색 배경 #f5f5f5):
  - 제목: "paper 그대로 유지"
  - bullet 1: "§V-A ECQO HNSW range query"
  - bullet 2: "§V-B Adaptive Sampling Eq 1-6"
  - bullet 3: "**cardinality 추정 알고리즘**"
  - 하단: "→ 본 발표는 간단 소개"
- 우측 column (50%, navy 배경 #1e3a5f, 흰 텍스트):
  - 제목: "본 연구의 개입 지점"
  - bullet 1: "Phase 1 분포 인지 stratification"
  - bullet 2: "Phase 2 표본 추출"
  - bullet 3: "Phase 3 결합 minimal"
  - 하단: "→ 본 발표의 핵심"
- 두 column 사이 가운데 작은 화살표 (navy, "augment" 라벨)

### speaker note
> "framing을 두 layer로 나눕니다. 왼쪽은 paper Exqutor가 그대로 둔 부분입니다. §V-A ECQO HNSW range query, §V-B Adaptive Sampling의 Eq 1-6, 그리고 cardinality 추정 알고리즘 자체. 모두 paper의 contribution이고 저희는 인정하고 소개만 합니다. 오른쪽이 본 연구가 손을 댄 지점입니다. paper §V-B가 표본을 unstratified Bernoulli random으로 뽑던 그 자리를, 분포 인지 stratification으로 교체했습니다. Phase 1 분포 인지 stratification, Phase 2 표본 추출, Phase 3 결합. 이 발표의 핵심은 오른쪽입니다."

---

## Slide 4 — paper § 흐름 (간단 소개)

### Layout
- 상단 제목: "paper Exqutor의 § 흐름" (Inter Bold, 80px, 회색)
- 좌측 박스 (회색 배경 #f5f5f5, 25%):
  - 제목: "§V-A ECQO"
  - 본문: "HNSW range query"
  - sub: "정확 cardinality (1-2ms)"
  - 하단: "(인덱스 있을 때)"
- 중앙 화살표 + sub: "본 발표 대상 아님" (회색, 작은 폰트)
- 우측 박스 (navy 배경, 흰 텍스트, 50%):
  - 제목: "§V-B Adaptive Sampling"
  - Eq 1-6 verbatim (작은 mono 폰트)
  - sub: "Bernoulli random N=385 + momentum 보정"
  - 하단: "(인덱스 없을 때)"
- 가장 아래 한 줄: "**paper가 그대로 둔 cardinality 추정 mechanism**" (navy)

### Eq 1-6 박스 내부
```
Eq 1: N = ⌈z²·P̂(1-P̂)/e²⌉ = 385
Eq 2: Ĉ = Σ matching × (1 − ratio)
Eq 3: δ = max(true_Q-err, 1/true_Q-err)
Eq 4: η ← m·η + (1−m)·δ/α
Eq 5: N_t+1 ← N_t × (1 + η·β·sign)
Eq 6: η ← η · γ
```

### speaker note
> "paper Exqutor는 두 갈래로 cardinality를 추정합니다. §V-A ECQO는 인덱스가 있을 때 HNSW range query로 1-2ms 안에 정확한 값을 얻습니다. §V-B Adaptive Sampling은 인덱스가 없을 때 Bernoulli random 표본 N=385개로 추정하고, Eq 1-6 momentum으로 표본 크기를 동적으로 보정합니다. 둘 다 paper 본인의 contribution이고, 본 발표에서는 그대로 인정하고 간단히 소개만 합니다."

---

## Slide 5 — 본 연구 3 phase 흐름

### Layout
- 상단 제목: "본 연구의 sample selection 3 phase" (Inter Bold, 80px, navy)
- 3 박스 가로 배치 (각 30%, 화살표 연결):

  **박스 1: Phase 1 (★ 개입)**
  - 제목: "Phase 1"
  - sub: "분포 인지 stratification"
  - bullet: "(offline, 1회)"
  - body:
    - "① Type 판별 (행 수/structure/차원)"
    - "② Type별 권장 method 선택"
    - "③ K=20 stratum 분할"
  - 하단 navy 라벨: "★ 본 연구 개입"

  **박스 2: Phase 2 (★ 개입)**
  - 제목: "Phase 2"
  - sub: "표본 추출"
  - bullet: "(online, 매 query)"
  - body:
    - "① budget N=385 (paper verbatim)"
    - "② 각 stratum 비례 추출"
    - "③ est_method 계산"
  - 하단 navy 라벨: "★ 본 연구 개입"

  **박스 3: Phase 3 (minimal)**
  - 제목: "Phase 3"
  - sub: "결합 minimal"
  - bullet: "(online, 매 query)"
  - body:
    - "① est_b1 (paper Bernoulli)"
    - "② est_method (본 연구)"
    - "③ **est_final = (b1+method)/2**"
  - 하단 회색 라벨: "minimal"

- 박스 3 아래 추가 화살표:
  ```
              ↓
  [paper Adaptive Eq 1-6 보정 (그대로 유지)]
  ```

### speaker note
> "본 연구의 흐름은 3 phase입니다. Phase 1과 Phase 2가 핵심 개입 지점입니다. Phase 1에서는 데이터셋을 적재할 때 한 번, 분포를 보고 K=20개 stratum으로 나눕니다. 어떤 method로 나눌지는 데이터셋 Type에 따라 자동 선택합니다. Phase 2에서는 쿼리가 들어올 때마다 각 stratum에서 비례적으로 표본을 뽑습니다. budget N=385는 paper Eq 1 그대로입니다. Phase 3 결합은 paper Bernoulli 추정값과 저희 추정값을 단순 산술 평균하는 minimal한 방식이고, 이후 paper의 Eq 1-6 momentum 보정은 그대로 둡니다."

---

## Slide 6 — 측정 portfolio

### Layout
- 중앙 거대 수치 "**1444건**" (Inter Bold, 300px, navy)
- 그 아래 sub: "B1 대조군 80 + CaseB 실험군 1364"
- 하단 표 (흐릿한 회색):
  - dataset: SIFT / SimSearchNet++ / DEEP / WIKI / YFCC + 직접 concat한 multi-vector
  - scale factor: 1, 10, 100
  - method: 16 (8 paradigm 대표)
  - selectivity: 0.001 / 0.01 / 0.10
- 우상단 mini badge (navy): "paired 1360 · 신뢰 비교 1240 (K=10 제외)"

### speaker note
> "본 측정 portfolio는 통합 1444건입니다. 대조군 B1이 80건, 실험군 CaseB가 1364건입니다. 같은 cell, selectivity, strata 조건에서 B1과 CaseB를 trial 단위로 짝지으면 paired 비교가 1360건 나옵니다. 그중 K=10 측정 120건은 대조군 B1 자체에 결함이 있어 제외했고, 신뢰 가능한 1240건이 분석의 근거입니다. 데이터셋 여러 종, scale factor 3종, method 16개, selectivity 3종으로 측정 공간을 펼쳤습니다."

---

## Slide 7 — ★ 데이터셋 + 우리의 concat 확장

### Layout
- 상단 제목: "측정 공간을 직접 확장하다" (Inter Bold, 80px, navy)
- 상단 부제 (작은 폰트): "공개 데이터셋 + 우리가 직접 concat한 multi-vector"
- 좌측 박스 "단일 벡터 데이터셋": SIFT / SimSearchNet++ / DEEP / WIKI / YFCC
- 우측 박스 (navy 강조) "우리가 직접 concat한 multi-vector":
  - "DEEP+SIFT (288d)"
  - "DEEP+WIKI (864d)"
  - "DEEP+YFCC (288d)"
  - 라벨: "두 데이터셋의 벡터를 차원 방향으로 연결"
- 하단 Type 분류 표 (5 row):

| Type | 정의 | 차원 | 측정 행 수 |
|---|---|---:|---:|
| **Type 1** | 소규모 단일 sf=1 | 96-768 | 263 |
| **Type 2** | 중규모 단일 sf=10 | 96-768 | 212 |
| **Type 3** | 대규모 단일 sf=100 | 96-256 | 439 |
| **Type 4a** | 대규모 다중 288d (우리 concat) | 288 | 369 |
| **Type 4b** | 대규모 다중 864d (우리 concat) | 864 | 161 |

### speaker note
> "본 연구는 공개 데이터셋을 측정하는 데 그치지 않았습니다. 단일 벡터 데이터셋은 SIFT, SimSearchNet++, DEEP, WIKI, YFCC를 썼고, 여기에 더해 두 데이터셋의 벡터를 차원 방향으로 직접 concat한 multi-vector 데이터셋을 만들어 측정 공간을 능동적으로 확장했습니다. DEEP과 SIFT를 합친 288차원, DEEP과 WIKI를 합친 864차원 같은 조합입니다. 이렇게 만든 데이터를 규모와 구조에 따라 다섯 Type으로 분류했습니다. 소규모 단일부터 우리가 직접 만든 864차원 다중 벡터까지입니다. 각 Type이 다음에 나올 dynamic 할당의 축이 됩니다."

---

## Slide 8 — ★ paradigm별 사용 16 method overview

### Layout
- 상단 제목: "**8 paradigm을 대표하는 16개 method**" (Inter Bold, 80px, navy)
- 7 paradigm 매트릭스:

| paradigm | method | count |
|---|---|---:|
| **P1 Cluster** | minibatch_partial / gmm / faiss_ivf | 3 |
| **P2 Spatial** | hilbert_real ★ / zorder_morton / skilling_hilbert | 3 |
| **P3 Streaming** | chao_weighted ★ | 1 |
| **P4 DimReduction** | sparse_rp ★ / pca1d ★ / rsvd / ica_fastica | 4 |
| **P5 QMC** | cum_sqrtf / lavallee_hidiroglou | 2 |
| **P6 Quantization** | rabitq_strat / mhist2 | 2 |
| **P9 InfoTheoretic** | hyperloglog ★ | 1 |

- 하단 ★ 라벨: "★ = 16개 측정 방법 중 Top 5 method (정확도 상위 = 자원 효율 상위)"
- 우측 박스 (navy): "**모두 sample selection 단계의 mechanism**"

### speaker note
> "본 연구가 측정한 method는 8개 sampling paradigm을 대표하는 16개입니다. Cluster 3개, Spatial 3개, Streaming 1개, DimReduction 4개, QMC 2개, Quantization 2개, InfoTheoretic 1개입니다. 별표가 붙은 다섯 개는 정확도와 자원 효율을 함께 고려한 Top 5 method입니다. 강조하면, 이 16개는 모두 표본 선택 단계에서 데이터를 어떻게 stratum으로 나누느냐 하는 mechanism입니다. cardinality 추정 알고리즘 자체가 아닙니다."

---

## Slide 9 — ★ P1 Cluster method 알고리즘

### Layout (3 column, 각 column = 1 method)

**Column 1: minibatch_partial** (Sculley 2010)
- 알고리즘: MiniBatchKMeans + `partial_fit` (sub-batch 단계별 학습)
- step diagram:
  ```
  input → batch 1 → partial_fit → centers
       → batch 2 → partial_fit → centers (update)
       → ... → final centers → 표본 → stratum_id
  ```
- 시간 복잡도: O(batch × K × d) per batch
- reference: "Sculley 2010 (KDD)"

**Column 2: gmm** (Dempster-Laird-Rubin 1977 EM)
- 알고리즘: Gaussian Mixture Model with EM
- step diagram:
  ```
  input → initial μ_k, Σ_k, π_k
       → E-step: P(z=k | x)
       → M-step: μ_k, Σ_k, π_k update
       → 수렴까지 iter → 표본 → stratum_id
  ```
- 본 측정: `covariance_type='diag'` + `reg_covar=1e-2` (SIFT 128d / SSN 256d cholesky fail 회피)
- reference: "Dempster-Laird-Rubin 1977 (JRSS)"

**Column 3: faiss_ivf** (Johnson-Douze-Jégou 2017)
- 알고리즘: IVF (Inverted File Index) — 벡터를 cluster center에 nearest 매핑
- step diagram:
  ```
  input → coarse quantizer (K-means K=20)
       → 벡터 → cluster 매핑
       → stratum_id = cluster id → 표본
  ```
- 시간 복잡도: O(n × K × d) train + O(n × log K) assign
- reference: "Johnson-Douze-Jégou 2017 (FAISS, IEEE)"

- 우상단 라벨 (navy): "표본을 cluster center에 매핑"

### speaker note
> "P1 Cluster paradigm은 세 method입니다. minibatch_partial은 Sculley 2010의 partial_fit으로 미니배치 K-means를 단계별로 학습합니다. gmm은 Dempster-Laird-Rubin 1977의 EM 알고리즘으로 Gaussian Mixture를 적합합니다. faiss_ivf는 Johnson-Douze-Jégou 2017의 IVF로 벡터를 가장 가까운 cluster center에 매핑합니다. 셋 다 표본을 cluster center에 매핑해 stratum_id를 부여하는 방식입니다."

---

## Slide 10 — ★ P2 Spatial method 알고리즘

### Layout (3 column)

**Column 1: hilbert_real ★** (Faloutsos 1989)
- 알고리즘: Hilbert curve indexing — 고차원 벡터를 1D scalar로 매핑
- step diagram:
  ```
  벡터 → PCA 2D/3D → Hilbert curve → 1D scalar
       → quantile 분할 K=20 → stratum_id
  ```
- 본 측정: raw Wikipedia Hilbert curve 표준 구현 (정직하게 명명된 구현 사용)
- reference: "Faloutsos 1989 (PODS)"
- ★ Top 5 method

**Column 2: zorder_morton** (Morton 1966)
- 알고리즘: Z-order space-filling curve — bit interleaving
- step diagram:
  ```
  벡터 → PCA 저차원 → 각 dim binary
       → bit interleaving → quantile 분할 → stratum_id
  ```
- 시간 복잡도: O(n × d × log d) bit ops
- reference: "Morton 1966 (IBM)"

**Column 3: skilling_hilbert** (Skilling 2004)
- 알고리즘: state-machine 기반 true 고차원 Hilbert curve
- step diagram:
  ```
  벡터 → unit cube 정규화 → state-machine 고차원 Hilbert
       → 1D scalar → quantile 분할 → stratum_id
  ```
- AIP Conf Proc 2004; 707:381-387 verbatim
- reference: "Skilling 2004 (AIP)"

- 우상단 라벨: "표본을 space-filling curve 1D scalar로"

### speaker note
> "P2 Spatial paradigm은 세 method입니다. hilbert_real은 Faloutsos 1989의 Hilbert curve로 고차원 벡터를 1차원 스칼라로 매핑하고, Top 5 method에 듭니다. zorder_morton은 Morton 1966의 Z-order curve로 비트 인터리빙을 합니다. skilling_hilbert는 Skilling 2004의 state-machine 알고리즘으로 진짜 고차원 Hilbert curve를 계산합니다. 셋 다 벡터를 space-filling curve로 1차원 스칼라로 만든 뒤 quantile로 나눠 stratum_id를 부여합니다."

---

## Slide 11 — ★ P3 Streaming method 알고리즘 (Top 5 method ★)

### Layout (1 method 상세, full slide)

**Method: chao_weighted ★** (Chao 1982 *Biometrika*)
- 알고리즘: Weighted reservoir sampling (priority queue 기반)
- step diagram (큰 박스, 좌→우):
  ```
  input 벡터
     → weight 부여 (예: norm 또는 anomaly score)
     → priority queue (size K=20)
     → weighted random 표본
     → K stratum 분포 인지 부여 → stratum_id
  ```
- 시간 복잡도: O(n × log K) priority queue insert/pop
- 메모리: O(K) priority queue — 매우 compact
- 본 측정: 단일 pass로 데이터셋 streaming 처리
- reference: "Chao 1982 — A general purpose unequal probability sampling plan (*Biometrika*)"
- ★ Top 5 method

- 우측 칼럼 (navy 박스):
  - 제목: "Top 5 method ★"
  - bullet: "메모리 O(K)로 compact"
  - bullet: "단일 pass streaming 친화"
  - bullet: "16 method 중 정확도 상위권 (mean Δ% −8.37%)"

### speaker note
> "P3 Streaming paradigm은 chao_weighted 한 method입니다. Chao 1982 Biometrika의 weighted reservoir sampling으로, priority queue를 기반으로 합니다. 각 벡터에 weight를 부여하고, 크기 K=20의 priority queue에서 weighted random 표본을 뽑아 stratum을 부여합니다. 시간은 O(n log K), 메모리는 O(K)로 매우 compact합니다. 단일 pass로 streaming 처리가 가능하고, 16개 method 중 평균 Δ% −8.37%로 정확도 상위권입니다. Top 5 method에 듭니다."

---

## Slide 12 — ★ P4 DimReduction method 알고리즘

### Layout (4 column, 각 column = 1 method)

**Column 1: sparse_rp ★** (Li-Hastie-Church 2006)
- 알고리즘: Very Sparse Random Projection
- step diagram:
  ```
  벡터 → sparse matrix (density ~1/√d)
       → random projection → projected 벡터
       → quantile 분할 K=20 → stratum_id
  ```
- 시간 복잡도: O(n × k × d × density)
- reference: "Li-Hastie-Church 2006 (KDD)"
- ★ Top 5 method

**Column 2: pca1d ★** (Pearson 1901)
- 알고리즘: PCA → 1D projection
- step diagram:
  ```
  벡터 → covariance matrix SVD
       → 1st principal component projection (1D scalar)
       → quantile 분할 K=20 → stratum_id
  ```
- 시간 복잡도: O(n × d²) covariance + O(d³) SVD
- reference: "Pearson 1901 (Phil. Mag.)"
- ★ Top 5 method

**Column 3: rsvd** (Halko-Martinsson-Tropp 2011)
- 알고리즘: Randomized SVD — 대규모 PCA
- step diagram:
  ```
  벡터 → random sketch (Gaussian)
       → low-rank approximation → power iter
       → truncated SVD → 1D projection → stratum_id
  ```
- 본 측정: pca1d의 대규모 대안
- reference: "Halko-Martinsson-Tropp 2011 (SIAM Rev.)"

**Column 4: ica_fastica** (Hyvärinen 1999)
- 알고리즘: FastICA — non-Gaussian 독립성
- step diagram:
  ```
  벡터 → centering + whitening
       → fixed-point iter → non-Gaussianity max
       → independent component → 1D projection → stratum_id
  ```
- 시간 복잡도: O(n × d²) iteration
- reference: "Hyvärinen 1999 (IEEE TNN)"

- 우상단 라벨: "저차원 projection → quantile 분할"

### speaker note
> "P4 DimReduction paradigm은 네 method입니다. sparse_rp는 Li-Hastie-Church 2006의 very sparse random projection입니다. pca1d는 Pearson 1901의 PCA로 첫 주성분에 1차원 투영합니다. rsvd는 Halko-Martinsson-Tropp 2011의 randomized SVD로 대규모 PCA를 다룹니다. ica_fastica는 Hyvärinen 1999의 FastICA로 non-Gaussian 독립 성분을 찾습니다. 넷 다 저차원으로 투영한 뒤 quantile로 나눠 stratum_id를 부여합니다. sparse_rp와 pca1d는 Top 5 method에 듭니다."

---

## Slide 13 — ★ P5 QMC method 알고리즘

### Layout (2 column, 각 column = 1 method)

**Column 1: cum_sqrtf** (Dalenius-Hodges 1959)
- 알고리즘: Minimum Variance Stratification (cumulative √f)
- step diagram:
  ```
  벡터 → 1D projection (예: PCA 1st PC)
       → cumulative √frequency 계산
       → K=20 등간격 분할 → stratum_id
  ```
- 본 측정: classical stratification 정통 (JASA 1959 verbatim)
- reference: "Dalenius-Hodges 1959 (JASA)"

**Column 2: lavallee_hidiroglou** (Lavallée-Hidiroglou 1988)
- 알고리즘: take-all stratum + Neyman allocation
- step diagram:
  ```
  벡터 → magnitude 정렬 (내림차순)
       → top-N take-all stratum (큰 값 전수)
       → 나머지 → Neyman allocation → stratum 분할 → stratum_id
  ```
- 본 측정: skew long-tail 데이터셋 효과 (Survey Methodology 1988 verbatim)
- reference: "Lavallée-Hidiroglou 1988 (Survey Methodology)"

- 우상단 라벨: "classical stratification mechanism"

### speaker note
> "P5 QMC paradigm은 두 method입니다. cum_sqrtf는 Dalenius-Hodges 1959의 cumulative √f 방식으로, 최소 분산 stratification의 정통 기법입니다. lavallee_hidiroglou는 Lavallée-Hidiroglou 1988의 take-all stratum과 Neyman allocation을 결합한 방식으로, 큰 값을 전수 조사하는 stratum을 따로 두어 skew된 long-tail 데이터셋에 효과적입니다. 둘 다 분포를 stratum으로 나누는 classical stratification mechanism입니다."

---

## Slide 14 — ★ P6 Quantization method 알고리즘

### Layout (2 column, 각 column = 1 method)

**Column 1: rabitq_strat** (Gao-Lin 2024 VLDB Best Paper)
- 알고리즘: RaBitQ — sign 기반 quantization
- step diagram:
  ```
  벡터 → random projection (Gaussian)
       → sign per dim (>0 → 1 / ≤0 → 0)
       → binary code → K=20 quantile 분할 → stratum_id
  ```
- 본 측정: 2024 VLDB Best Paper
- reference: "Gao-Lin 2024 (VLDB Best Paper)"

**Column 2: mhist2** (Poosala 1997 VLDB)
- 알고리즘: MHIST — 다차원 histogram
- step diagram:
  ```
  벡터 → d차원 multi-dim histogram bucket
       → max variance split iter
       → bucket id → stratum_id
  ```
- 본 측정: histogram 기반 대안
- reference: "Poosala 1997 (VLDB)"

- 우상단 라벨: "quantization 기반 stratum 부여"

### speaker note
> "P6 Quantization paradigm은 두 method입니다. rabitq_strat은 Gao-Lin 2024 VLDB Best Paper의 RaBitQ로, 차원마다 부호를 보고 binary code를 만들어 quantization합니다. mhist2는 Poosala 1997 VLDB의 MHIST로 다차원 histogram bucket을 분산이 최대인 방향으로 분할합니다. 둘 다 quantization 결과를 stratum_id로 부여합니다."

---

## Slide 15 — ★ P9 InfoTheoretic method 알고리즘 (Top 5 method ★)

### Layout (1 method 상세, full slide)

**Method: hyperloglog ★** (Flajolet-Fusy-Gandouet-Meunier 2007 AofA)
- 알고리즘: HyperLogLog — cardinality estimation sketch (본 연구는 이 sketch를 stratum 부여에 활용)
- step diagram (큰 박스, 좌→우):
  ```
  벡터 → hash function → uniform random bit
       → leading zero count
       → register (m buckets) max update
       → harmonic mean → stratum 부여 → stratum_id
  ```
- 본 측정: sketch 기법 정통 (AofA 2007 verbatim)
- 메모리: O(m × log log n) — 매우 compact
- reference: "Flajolet-Fusy-Gandouet-Meunier 2007 (AofA)"
- ★ Top 5 method

- 우측 칼럼 (navy 박스):
  - 제목: "Top 5 method ★"
  - bullet: "메모리 O(m × log log n)로 매우 compact"
  - bullet: "sketch 기법 정통"
  - bullet: "16 method 중 정확도 상위권 (mean Δ% −7.73%)"

### ★ 주의 (speaker note에서 강조)
> "주의할 점이 하나 있습니다. HyperLogLog는 원래 cardinality를 추정하는 sketch 알고리즘으로 잘 알려져 있습니다. 하지만 본 연구에서는 그 sketch의 결과를 cardinality 추정값으로 쓰는 것이 아니라, 표본을 stratum으로 나누는 입력으로 활용합니다. 즉 HyperLogLog는 본 연구에서 sample selection 단계의 도구이지, cardinality 추정 알고리즘이 아닙니다."

### speaker note
> "P9 InfoTheoretic paradigm은 hyperloglog 한 method입니다. Flajolet 외 2007 AofA의 HyperLogLog로, 해시값의 leading zero를 세고 register를 갱신한 뒤 조화 평균을 냅니다. 한 가지 주의할 점은, HyperLogLog는 원래 cardinality 추정 sketch로 알려진 알고리즘이라는 것입니다. 본 연구에서는 그 결과를 cardinality 추정값으로 쓰지 않고, 표본을 stratum으로 나누는 입력으로만 활용합니다. 메모리가 O(m log log n)로 매우 compact하고, 평균 Δ% −7.73%로 정확도 상위권이라 Top 5 method에 듭니다."

---

## ☆ Part 2/3 끝 — Part 3/3 (slide 16-25 detail + 결론) 이어서 paste

작성: 2026-05-17 KST · slide 1-15 상세. paradigm별 method 알고리즘 step diagram + reference + Top 5 method 표시. speaker notes 전면 정상 한국어로 다시 씀 ("영역" 필러 0회). hilbert_real·sparse_rp는 REPORT v12 §9-(6)에 따라 "정직하게 명명된 구현" 표현 사용.
