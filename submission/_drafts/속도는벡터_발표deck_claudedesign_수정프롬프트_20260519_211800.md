# claude.ai/design 수정 프롬프트 — 발표 deck(19장) 디자인 결함 수정

> 작성 2026-05-19 21:18 KST · v17-print 비교 반영 갱신 21:36 · deck을 생성한 claude.ai/design 같은 대화에 이어 복붙하는 수정 지시
> 원본 생성 프롬프트: 속도는벡터_발표deck_claudedesign_프롬프트_20260519_191338.md
> 검증 대상(현재·결함본): 속도는벡터_최종발표_슬라이드_20260519_205550.pptx = **v18** · 레이아웃 기준: **v17-print**(이전 deck)
> 핵심 한 줄: v18의 새 컬러풀 디자인은 그대로 두고, 리스킨 과정에서 텍스트가 줄바꿈·겹침으로 깨진 슬라이드만 v17-print의 깔끔한 레이아웃 수준으로 되돌린다.

---

## 사용법 — ★ 먼저 읽기

- **어느 대화에 붙이나** — 이 파일은 deck을 생성·리스킨했던 claude.ai/design 대화(Keynote_Capstone)에 이어서 복붙하는 수정 지시다. 그 대화에는 **v17-print(이전 deck)와 v18(현재 deck) 두 버전**이 있다.
- **★ v18 색은 목표, v17-print 레이아웃은 목표** — v18은 색을 다양하게 입혀 스타일리시해진 버전이고 이 방향은 옳다(유지). 그런데 리스킨 과정에서 글자가 커지고 박스가 좁아지며 **텍스트가 두 줄로 끊기거나 다른 요소와 겹치는 결함**이 여러 슬라이드에 생겼다. v17-print는 같은 19장을 절제된 색으로 담은 버전으로, **텍스트가 한 줄로 들어가고 겹침이 없는 깔끔한 레이아웃**이다. 이 수정은 — v18의 색·악센트·그라데이션은 그대로 두고, 깨진 슬라이드의 레이아웃만 v17-print 수준으로 되돌린다. **v18 = 색 기준, v17-print = 레이아웃 기준.**
- **targeted fix** — 19장 구성·제목·순서·텍스트·수치는 동결한다. 아래 「발견된 결함」에 적힌 슬라이드만 손본다. 결함으로 지목하지 않은 슬라이드·요소는 v18 그대로 둔다 — 그 슬라이드들은 v17-print와 레이아웃이 이미 같아 손댈 필요가 없다.
- **코드블록 안 내용만 복붙** — 아래 ` ```text ``` ` 코드블록 **안 내용만** claude.ai/design 대화창에 복붙한다. 블록 밖 글은 팀원을 위한 안내다.
- **왜 2개 코드블록으로 나누나** — 결함이 19장에 걸쳐 있다. 한 번에 다 시키면 claude.ai/design이 생성 중 일탈한다. 슬라이드 1~10 / 11~19로 끊고, 각 블록 끝 자가검증으로 일탈을 일찍 잡는다.
- **슬라이드는 제목으로 가리킨다** — 화면에 번호가 없으므로 코드블록은 슬라이드를 제목으로 부른다.
- 코드블록 2 끝에서 19장 .pptx를 재-export한다.

---

## 절대 규칙 (수정 후에도 지킬 것)

- **내용·구조·수치 동결** — 19장 구성·제목·순서·모든 텍스트·모든 수치를 한 자도 바꾸지 않는다(단 s9의 "영역"→"구간" 교체, s6의 "÷2"→"평균" 라벨 도식 교체는 아래 지시된 의도된 수정).
- **새 컬러풀 디자인(v18) 유지** — navy 앵커 `#1E3A5F` + 악센트 4색(청록 `#0EA5E9`·코랄 `#F97316`·그린 `#10B981`·바이올렛 `#8B5CF6`) + 개념색(기존 방식 그레이 `#94A3B8`·완전 대체 코랄·결합 청록) + 거대 수치 그라데이션. 색을 v17-print의 절제된 톤으로 되돌리지 않는다.
- **코드명 금지** — B1·CaseA·CaseB·qe_trim 같은 표기는 어디에도 쓰지 않는다. 세 방식은 "기존 방식 / 완전 대체 / 결합"으로만 부른다.
- **수식·코드 용어·영문 약어 금지** — ÷2 같은 수식 표기, 영문 약어(GMM 등), 페이지 번호·"★" 표기를 노출하지 않는다.
- **"영역" 필러 단어 금지** — 무의미한 "영역"을 문장에 채워 넣지 않는다. 자리는 "구간"으로 쓴다.
- **잘림·겹침 0** — 어떤 박스·그림·막대·텍스트도 슬라이드 경계 밖으로 잘리거나 다른 요소와 겹치지 않는다.

---

## 발견된 결함 (이 수정으로 전부 잡는다)

메인 세션이 v18(현재) 19장을 v17-print(이전)와 한 장씩 대조해 가려낸 결함이다. **v17-print에도 있는 디자인(여백 분포·맨 텍스트 라벨·팀명 표기 등)은 의도된 것이므로 결함이 아니다 — 아래 표에 없는 슬라이드는 손대지 않는다.** 결함은 세 갈래다 — ① 리스킨이 깨뜨린 텍스트 줄바꿈·겹침(v17-print엔 없음, 반드시 회복) ② 이전부터 있던 절대 규칙 위반(수식·필러, 함께 정리) ③ 새 디자인 시스템 정합(워드마크 그라데이션).

| 슬라이드 | 유형 | 결함 | v17-print 기준 |
|---|---|---|---|
| s1 표지 | 리스킨 깨짐 | 팀원 줄 "이동욱"이 다음 줄로 줄바꿈 | v17-print는 네 명 한 줄 |
| s1·s19 | 디자인 정합 | 워드마크 "벡터"·상단 바 그라데이션 없음(solid navy) | 새 시스템 적용 — 팜플렛·포스터엔 이미 navy→청록 그라데이션 |
| s2 왜 카디널리티 추정이 중요한가 | ★리스킨 깨짐 | 중앙 설명 텍스트가 33%/100% 박스 위로 겹침 | v17-print는 흐름도/설명/박스가 세로로 분리·겹침 0 |
| s3 논문이 푼 것, 우리가 바꾼 것 | ★리스킨 깨짐 | 태그 "우리가 바꾼 곳"의 "곳" 삐져나옴 · "논문 그대로" 태그·패널 제목·"분포 인지" 미니박스 2줄 | v17-print는 태그·제목·라벨 전부 한 줄 |
| s4 연구가 걸어온 길 | 리스킨 깨짐 | 타임라인 3노드 제목이 각 2줄 | v17-print는 각 한 줄 |
| s6 세 가지 방식을 같은 조건으로 비교했다 | 규칙 위반 | 결합 카드 도식에 "÷2" 수식 노출 | 같은 deck의 s5는 "논문 추정/우리 추정→평균" 라벨 도식 — 그것과 일관되게 |
| s9 데이터가 쏠려 있을 때… | ★규칙 위반 | "영역" 필러 단어 3~4회 반복 | "구간"으로 교체 |
| s10 16가지 방법, 7가지 접근 | 경미 | "가우시안 혼합 모델(GMM)"의 "(GMM)" 영문 약어 노출 | 한국어 이름만 |
| s12 완전 대체는 불안정 — 결합이 답 | ★리스킨 깨짐 | 막대 라벨 "완전 대체"가 2줄로 끊기며 코랄 막대 위로 겹침 | v17-print는 "완전 대체"·"결합" 라벨이 각 막대 위 한 줄·겹침 0 |
| s14 선택도·데이터 구조·계층 수 | 리스킨 깨짐 | "데이터 구조" 패널 "다중 벡터" 라벨 2줄("단일 벡터"는 1줄) | v17-print는 둘 다 한 줄 |
| s16 데이터 유형을 보고 방법을 자동으로 고른다 | ★리스킨 깨짐 | 흐름 박스 2의 "데이터 유형 판별"과 "크기·구조·차원"이 겹침/충돌 | v17-print는 "데이터 유형 판별"(주)+"크기·구조·차원"(보조)이 깔끔히 분리 |
| s17 결론 | 안전 확인 | 하단 캡션 박스가 슬라이드 맨 아래에 눌려 잘릴 위험 | v17-print는 캡션이 슬라이드 안에 잘림 없이 |
| s18 한계와 다음 단계 | 리스킨 깨짐 | 좌측 헤더 "정직하게 남기는 점" 2줄 · 우측 두 박스 라벨 "산업 적용"·"논문 적용" 2줄+아이콘 겹침 | v17-print는 헤더·라벨 전부 한 줄 |

수치는 모두 정확하고(89.1%·1344/1508·4.4%·65%·72%·35.2%·96.5%·13/16·83.3/87.6/97.5·89.1/89.2·83.6/89.8/85.9·1508·33%/100%) 코드명도 0이다. **s5·s7·s8·s11·s13·s15은 v17-print와 레이아웃이 같아 손대지 않는다.** s1의 작은 회색 "속도는벡터"는 v17-print에도 있는 팀명 표기이므로 결함이 아니다 — 제거하지 않는다.

---

## 수정 지시 — 코드블록 1 (슬라이드 1~10)

아래 코드블록 안 내용을 claude.ai/design 대화창에 복붙한다.

```text
직전에 만든 발표 deck을 부분 수정할 거야. 이 대화에는 v17-print(이전 버전)와 v18(지금 버전) 두 deck이 있지. v18은 색을 다양하게 입혀 스타일리시해졌고 그 방향은 그대로 두고 싶어 — 다만 v18로 오면서 글자가 커지고 박스가 좁아져서 텍스트가 두 줄로 끊기거나 다른 요소와 겹친 슬라이드가 몇 개 생겼어. v17-print는 같은 19장인데 텍스트가 한 줄로 깔끔히 들어가 있어. 이 수정은 v18의 색은 그대로 두고, 깨진 슬라이드의 레이아웃만 v17-print처럼 깔끔하게 되돌리는 거야. 슬라이드 1~10을 먼저 손본다. 11~19는 다음 메시지로 줄게.

[원칙]
- v18의 색·악센트·그라데이션은 그대로 둬. 색을 v17-print의 절제된 톤으로 되돌리지 마. 고치는 건 레이아웃(줄바꿈·겹침)뿐이야.
- 19장 구성·제목·텍스트·수치는 한 자도 바꾸지 마. 슬라이드를 추가·삭제하지 마.
- 아래에 지목하지 않은 슬라이드('분포를 반영해 표본을 뽑는 세 단계', '무엇을, 어떤 조건으로 측정했는가', '측정한 데이터셋')는 v17-print와 레이아웃이 이미 같으니 손대지 마.
- 슬라이드는 화면에 번호가 없으니 제목으로 가리킨다.

[슬라이드 '표지']
- 하단 팀원 줄이 v18에서 '박세은 · 강재현 · 조현빈 ·'에서 끊기고 '이동욱'이 다음 줄로 넘어간다. v17-print처럼 네 명 '박세은 · 강재현 · 조현빈 · 이동욱'이 한 줄에 다 들어오게 해 — 그 줄 폭을 넓히거나 글자 크기를 약간 줄여서. (팀원 줄 아래 작은 회색 '속도는벡터'는 v17-print에도 있는 팀명 표기다 — 그대로 둬.)
- 워드마크 '속도는벡터'에서 '벡터' 두 글자를 navy에서 청록(#0EA5E9)으로 가는 그라데이션으로 바꿔(왼쪽 navy → 오른쪽 청록). '속도는' 세 글자는 navy 그대로. 워드마크 위 짧은 밑줄 바도 같은 navy→청록 그라데이션으로. (팜플렛·포스터 표지와 통일된다.)

[슬라이드 '왜 카디널리티 추정이 중요한가'] ★
- v18에서 흐름도 아래 중앙 설명 텍스트('조건을 만족하는 행의 수를 잘못 추정하면 →' / '잘못된 실행 계획 → 느린 쿼리')가 그 아래 33%·100% 박스 위로 겹쳐서 글자가 박스와 포개져 읽을 수 없다.
- v17-print를 보면 이 슬라이드는 위에서 아래로 ① 흐름도 ② 중앙 설명 텍스트 ③ 33%·100% 두 박스가 세로로 또렷이 분리돼 있고 겹침이 전혀 없다. v18도 그렇게 세 블록을 세로로 분리해, 어떤 글자도 박스를 침범하지 않게 해. 세 블록을 슬라이드 세로 중앙에 균형 있게 모은다.
- 흐름도 끝 '빠른 쿼리'·'느린 쿼리'가 박스 없는 맨 텍스트인 건 v17-print도 그렇다 — 그대로 둬.

[슬라이드 '논문이 푼 것, 우리가 바꾼 것'] ★
- v18에서 중앙 패널 위 태그 '우리가 바꾼 곳'에서 '곳' 글자가 태그 배경 밖으로 삐져나왔다. 우측 패널 위 태그 '논문 그대로'는 2줄로 끊겼다. 패널 제목 '표본 뽑기'·'카디널리티 추정'과 중앙 패널 안 미니박스 라벨 '분포 인지'도 2줄로 끊겼다.
- v17-print를 보면 이 태그 두 개, 패널 제목들, 미니박스 라벨이 전부 한 줄로 깔끔히 들어가 있다. v18도 — 태그 배경 폭을 글자에 맞게 키우고, 패널 제목·미니박스 라벨의 폰트 크기나 박스 폭을 조정해 — 전부 한 줄로 만들어. 글자가 배경을 넘치거나 어색하게 끊기지 않게 한다.

[슬라이드 '연구가 걸어온 길']
- v18에서 타임라인 3개 노드 제목 '연구 시작'·'중간 발표'·'최종 발표'가 각각 2줄('연구'/'시작' 식)로 끊겼다. v17-print는 셋 다 한 줄이다 — v18도 각 노드 라벨 폭을 넓혀 셋 다 한 줄로 만들어.

[슬라이드 '세 가지 방식을 같은 조건으로 비교했다']
- 세 번째 '결합' 카드 안 도식에 '÷2'라는 수식 기호가 들어가 있다 — 발표물에 수식 노출은 금지다. 같은 deck의 '분포를 반영해 표본을 뽑는 세 단계' 슬라이드를 보면 결합 단계가 '논문 추정'·'우리 추정' 두 칩이 화살표로 모여 '평균'으로 합쳐지는 라벨 도식으로 깔끔하게 그려져 있다. 이 '세 가지 방식을…' 슬라이드의 결합 카드 도식도 똑같이 바꿔 — '논문 추정값'·'우리 추정값' 두 칩이 화살표로 모여 '평균' 라벨로 합쳐지는 도식으로. '÷2' 표기는 제거하고 '평균'이라는 한국어 라벨만 남긴다. 카드 색·틴트·설명 문구는 그대로 둔다.

[슬라이드 '데이터가 쏠려 있을 때, 무작위는 쏠린 곳만 뽑는다'] ★
- 이 슬라이드에 '영역'이라는 무의미한 단어가 반복돼 있다 — 금지 단어다. 다음을 전부 '구간'으로 바꿔:
  · 좌측 패널 안 '드문 영역 — 표본 없음' → '드문 구간 — 표본 없음'
  · 우측 패널 안 '전 영역에서 골고루' → '전 구간에서 골고루'
  · 우측 패널 안 '드문 영역도 표본에 들어온다' → '드문 구간도 표본에 들어온다'
  · 하단 캡션 '분포를 반영하면 드문 영역까지 표본에 들어와 추정이 정확해진다' → '분포를 반영하면 드문 구간까지 표본에 들어와 추정이 정확해진다'
- '영역'이 또 보이면 전부 '구간'으로 바꿔. 레이아웃은 v17-print와 같아 손댈 필요가 없으니 텍스트만 교체한다.

[슬라이드 '16가지 방법, 7가지 접근']
- '클러스터링' 카드 안 방법 목록의 '가우시안 혼합 모델(GMM)'에서 영문 약어 '(GMM)'를 제거해 — '가우시안 혼합 모델'만 남긴다. 이 슬라이드는 그 외에는 v17-print와 같으니 더 손대지 마.

★ 색·수치·문구·슬라이드 수는 한 자도 바꾸지 마. v18의 컬러풀 디자인은 유지한다. 코드명·수식·영문 약어를 노출하지 마.

→ 자가검증: ① 표지 팀원 네 명이 한 줄인가? '벡터'와 워드마크 위 바가 navy→청록 그라데이션인가? ② '왜 카디널리티 추정이 중요한가'에서 중앙 설명 텍스트가 33%·100% 박스와 더 이상 겹치지 않고 v17-print처럼 세로로 분리됐나? ③ '논문이 푼 것, 우리가 바꾼 것'의 태그 두 개·패널 제목·'분포 인지' 미니박스가 모두 한 줄인가? ④ '연구가 걸어온 길' 타임라인 3노드 제목이 한 줄인가? ⑤ '세 가지 방식을 같은 조건으로 비교했다'의 '÷2'가 '평균' 라벨 도식으로 바뀌었나? ⑥ '데이터가 쏠려 있을 때…'에서 '영역'이 모두 '구간'으로 바뀌었나? ⑦ '16가지 방법, 7가지 접근'에서 '(GMM)'가 사라졌나? 문제가 있으면 고치고 다시 점검해.
```

**코드블록 1 후 할 일** — claude.ai/design 자가검증이 통과하면 코드블록 2로.

---

## 수정 지시 — 코드블록 2 (슬라이드 11~19)

코드블록 1의 자가검증이 통과한 뒤, 아래 코드블록 안 내용을 같은 대화에 이어 복붙한다.

```text
좋아, 같은 deck의 슬라이드 11~19를 손본다. 마찬가지로 v18의 색은 그대로 두고, 깨진 레이아웃만 v17-print처럼 되돌리는 거야. 19장 구성·제목·텍스트·수치는 한 자도 바꾸지 마. 아래에 지목하지 않은 슬라이드('핵심 결과 — 결합이 89.1%에서 개선', '방법별 정확도 — 16가지 중 13가지 우월', '자원 효율')는 v17-print와 레이아웃이 같으니 손대지 마.

[슬라이드 '완전 대체는 불안정 — 결합이 답'] ★
- v18에서 위쪽 코랄 막대 왼쪽 라벨 '완전 대체'가 '완전'/'대체' 2줄로 끊기면서 코랄 막대 위로 겹쳐 있다.
- v17-print를 보면 '완전 대체'와 '결합' 라벨이 각각 자기 막대 바로 위에 한 줄로 깔끔히 놓여 있고 막대와 겹치지 않는다. v18도 두 라벨을 각 막대 위에 한 줄로 올려, 막대와 글자가 전혀 겹치지 않게 해. 막대 색(완전 대체 코랄·결합 청록)과 수치(35.2%·89.1%·96.5%)는 그대로.

[슬라이드 '선택도·데이터 구조·계층 수 — 모든 조건에서 일관']
- v18에서 가운데 '데이터 구조' 패널의 막대 라벨 '다중 벡터'가 '다중'/'벡터' 2줄로 끊겼다. 같은 패널 '단일 벡터'는 한 줄이라 둘이 어긋난다. v17-print는 '단일 벡터'·'다중 벡터' 둘 다 한 줄이다 — v18도 '다중 벡터'를 한 줄로 맞춰('단일 벡터'와 같은 글자 크기·라벨 폭으로). 세 패널의 막대·수치 색은 그대로.

[슬라이드 '데이터 유형을 보고 방법을 자동으로 고른다'] ★
- v18에서 좌측 흐름 박스 4개 중 두 번째 박스의 '데이터 유형 판별'이라는 글자와 보조 텍스트 '크기·구조·차원'이 서로 겹쳐 충돌하고 있다.
- v17-print를 보면 두 번째 박스가 '데이터 유형 판별'(주 텍스트)과 그 아래 작은 보조 텍스트 '크기 · 구조 · 차원'으로 겹침 없이 깔끔하게 두 줄로 분리돼 있다. v18도 그렇게 — '데이터 유형 판별'과 '크기 · 구조 · 차원'을 세로로 단정히, 겹치지 않게 배치해. 흐름 박스 4개 모두 텍스트가 박스 안에 단정히 들어가는지 확인해.

[슬라이드 '결론']
- v18에서 하단 캡션 박스('추정 알고리즘은 그대로 두고, 표본을 뽑는 방식만 바꾸는 것만으로…')가 슬라이드 맨 아래 가장자리에 눌려 잘릴 위험이 있다. v17-print는 이 캡션이 슬라이드 경계 안에 여유 있게 들어와 있다. v18도 하단 캡션 박스가 슬라이드 안에 잘림 없이 들어오도록, 캡션을 약간 위로 올리거나 2×2 카드 그룹 높이를 약간 줄여. (2×2 카드 안 배치는 v17-print와 같으니 그대로 두고 — 하단 캡션 잘림만 본다.)

[슬라이드 '한계와 다음 단계']
- v18에서 좌측 박스 헤더 '정직하게 남기는 점'이 '정직하게 남기는'/'점' 2줄로 끊겼다. 우측 두 박스 라벨 '산업 적용'·'논문 적용'도 '산업'/'적용' 식으로 끊기며 옆 아이콘과 겹쳐 있다.
- v17-print는 이 헤더와 두 라벨이 전부 한 줄이다 — v18도 좌측 헤더 '정직하게 남기는 점'을 한 줄로, 우측 '산업 적용'·'논문 적용' 라벨을 각각 한 줄로 만들고 아이콘과 겹치지 않게 배치해. (우측 두 박스가 흰 배경인 건 v18이 맞다 — 그대로 둬.)

[슬라이드 '마무리']
- 상단 짧은 바가 navy 단색이다 — 표지와 동일하게 navy에서 청록(#0EA5E9)으로 가는 그라데이션으로 바꿔.

★ 색·수치·문구·슬라이드 수는 한 자도 바꾸지 마. v18의 컬러풀 디자인은 유지한다. 코드명·수식을 노출하지 마.

→ 자가검증: ① '완전 대체는 불안정 — 결합이 답'의 '완전 대체'·'결합' 라벨이 각 막대 위에 한 줄로 있고 막대와 겹치지 않나? ② '선택도·데이터 구조·계층 수…'의 '다중 벡터'가 한 줄인가? ③ '데이터 유형을 보고 방법을 자동으로 고른다'의 두 번째 흐름 박스에서 '데이터 유형 판별'과 '크기·구조·차원' 겹침이 사라졌나? 흐름 박스 4개 텍스트가 모두 박스 안에 단정한가? ④ '결론'의 하단 캡션이 슬라이드 안에 잘림 없이 들어왔나? ⑤ '한계와 다음 단계'의 좌측 헤더와 우측 두 라벨이 한 줄이고 아이콘과 겹치지 않나? ⑥ '마무리' 상단 바가 그라데이션인가? 문제가 있으면 고치고 다시 점검해.

(자가검증 통과 후) 19장 전체를 PowerPoint(.pptx) 파일로 다시 export해줘. 파일명은 '속도는벡터_최종발표_슬라이드'.
```

**코드블록 2 후 할 일** — export한 `.pptx`를 `Capstone/submission/_drafts/`에 저장 → 아래 검증 체크리스트로 최종 점검.

---

## 검증 체크리스트 (수정·export 후 점검)

export한 `.pptx`를 받아 아래를 점검한다. 결함별 해소 + 동결 확인.

### 결함별 해소
- [ ] **s1** — 팀원 네 명 한 줄 · 워드마크 "벡터"·상단 바 navy→청록 그라데이션. (작은 회색 "속도는벡터" 팀명 표기는 그대로 — 제거 대상 아님.)
- [ ] **s2** — 중앙 설명 텍스트가 33%/100% 박스와 겹치지 않음 · 흐름도/설명/박스 세로 분리.
- [ ] **s3** — "우리가 바꾼 곳"·"논문 그대로" 태그 한 줄(텍스트 안 넘침) · 패널 제목·"분포 인지" 미니박스 한 줄.
- [ ] **s4** — 타임라인 3노드 제목("연구 시작"·"중간 발표"·"최종 발표") 각 한 줄.
- [ ] **s6** — "÷2" 제거, "논문 추정값/우리 추정값 → 평균" 라벨 도식(s5와 일관).
- [ ] **s9** — "영역" 0회(전부 "구간"으로 교체).
- [ ] **s10** — "(GMM)" 제거.
- [ ] **s12** — "완전 대체"·"결합" 라벨이 각 막대 위 한 줄, 막대와 겹침 0.
- [ ] **s14** — "다중 벡터" 한 줄("단일 벡터"와 일치).
- [ ] **s16** — 흐름 박스 2 "데이터 유형 판별"·"크기·구조·차원" 겹침 0 · 흐름 박스 4개 텍스트 박스 안 단정.
- [ ] **s17** — 하단 캡션 박스 잘림 0(슬라이드 경계 안).
- [ ] **s18** — 좌측 헤더 "정직하게 남기는 점" 한 줄 · 우측 두 박스 라벨 한 줄 + 아이콘 겹침 0.
- [ ] **s19** — 상단 바 navy→청록 그라데이션.

### 동결·디자인 시스템 핵심
- [ ] 총 **19장**, 제목·순서 동결. 슬라이드 추가·삭제·통합 없음.
- [ ] 모든 텍스트·수치가 수정 전과 동일(s9 "영역"→"구간", s6 "÷2"→"평균" 라벨 도식만 의도된 수정 — 그 외 불변).
- [ ] **v18 컬러풀 디자인 유지** — navy 앵커 + 악센트 4색 + 개념색 3종 + 거대 수치 그라데이션. 색을 v17-print의 절제된 톤으로 되돌리지 않음.
- [ ] **s5·s7·s8·s11·s13·s15는 v18 그대로**(손대지 않음 — v17-print와 레이아웃 동일).
- [ ] 코드명·수식(÷2)·영문 약어(GMM)·페이지 번호·"★" 표기 없음.
- [ ] 잘림·겹침 0 — 어떤 박스·그림·막대·텍스트도 슬라이드 경계 밖으로 잘리거나 겹치지 않음.

### 4종 세트 정합
- [ ] deck의 개념색·악센트색·워드마크 그라데이션이 포스터·팜플렛·소개영상과 같은 시스템.

---

## paste 절차 (요약)

1. v17-print·v18 두 deck이 있는 claude.ai/design 대화(Keynote_Capstone)를 연다.
2. **코드블록 1**(슬라이드 1~10) 안 내용을 복붙 → claude.ai/design이 수정 + 자가검증.
3. 자가검증이 통과하면 **코드블록 2**(슬라이드 11~19) 안 내용을 같은 대화에 이어 복붙 → 수정 + 자가검증 → `.pptx` 재-export(파일명 "속도는벡터_최종발표_슬라이드").
4. export한 `속도는벡터_최종발표_슬라이드.pptx`를 `Capstone/submission/_drafts/`에 저장 → 위 검증 체크리스트로 최종 점검.

---

## 부록 — 불변 수치 표 (한 자리도 바꾸지 말 것 · v13 측정 정본)

| 항목 | 값 |
|---|---|
| 결합이 기존 방식보다 정확한 비율 | **89.1%** (1508번 중 1344번) |
| 추정 오차 중앙값 감소 | 약 **4.4%** |
| 통계적으로 의미 있는 개선 / 개선 폭 큰 경우 | **65% / 72%** |
| 완전 대체가 기존보다 나은 비율 | **35.2%** |
| 결합 vs 완전 대체 직접 비교 — 결합 우월 | **96.5%** |
| 방법별 정확도 | **16가지 중 13가지 우월**, 3가지 제외(가우시안 혼합 모델 · 미니배치 K-means · IVF 클러스터링) |
| 선택도별 | 0.1% **83.3%** / 1% **87.6%** / 10% **97.5%** |
| 데이터 구조별 | 단일 벡터 **89.1%** / 다중 벡터 **89.2%** |
| 계층 수별 | 10개 **83.6%** / 20개 **89.8%** / 30개 **85.9%** (20개 최적) |
| 데이터셋 | **9종 · 96~1024차원** |
| 측정 설계 5축 | 데이터셋 9종 · 규모 10만·100만·1000만 · 선택도 0.1·1·10% · 방법 16가지 · 계층 10·20·30개 |
| 총 측정 | **1508번** |

※ 위 표 외 수치를 새로 만들지 않는다. 모든 수치는 `_internal/cache/rq3/v13_summary.md` 정본 대조.

---

작성 2026-05-19 21:18 KST · v17-print 비교 반영 갱신 21:36 · claude.ai/design 발표 deck(19장) 디자인 결함 수정 프롬프트 · v18(현재)을 v17-print(레이아웃 기준)에 맞춰 텍스트 줄바꿈·겹침 회복, v18 색은 유지 · 모든 수치 v13 측정 정본 대조
